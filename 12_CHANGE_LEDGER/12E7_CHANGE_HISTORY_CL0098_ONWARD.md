> **Last updated:** Sunday, 20th September, 2026 9:40 PM
> **Granular part E7 (10 of 13) of `12_CHANGE_LEDGER.md`** — PART 3 change history: CL-0098 onward (continued) — THE OPEN PART: APPEND NEW ENTRIES HERE.
> All sibling parts live in `12_CHANGE_LEDGER/`; see `INDEX.md` at the repo root. Body below is verbatim source-of-truth content.
> *The table header row below is repeated for readability; the rows continue the PART 3 history table. Opened 20 September 2026 when `12E6` reached the 30 KB soft limit and was closed at CL-0097 (`CLAUDE.md` §4).*

<!-- KB-PART-BODY-START -->
| ID | Date | Change summary | Scope | Affected file(s) / rule(s) | Source (intake channel) | Class | Status | Authority decision |
|----|------|----------------|-------|----------------------------|--------|-------|--------|--------------------|
| CL-0098 | Sunday, 20th September, 2026 | **`_project_instructions_.md` CORRECTED — the Comparison Mode output description was stale in two ways, and ABSOLUTE RULE 2 in a third.** The project's system-prompt text described Mode 3's deliverable as **'two downloadable difference reports (Report 1 — Template-Derived; Report 2 — Knowledge-Derived), each bundle ending with the three-option IMPORTANT scope block'**. **BOTH HALVES CONTRADICTED THE KB.** There has never been a template-derived report: `09A` → PURPOSE and `09B` §5's inclusion gate **filter template-derived differences out and never report them**, precisely because refining the project files cannot change what an external template ships. And `09C` §7 carries **five** scope options (a series+level · b module-series · c universal · d ignore once · e ignore always), not three. The two report files that do exist are the **two passes**: `09C` §6 Phase 1 streamlined (three-section bundles, the five options listed once at the top, rule citations withheld) and, after the designer's `number-letter` pairings, `09C` §9 Phase 2 finalized (four sections each, adding the cited source rule, plus each scope in full English). **WHY THIS MATTERED:** the system prompt is loaded into every conversation ahead of the knowledge files, so a reader who trusted it would look for a template-derived report that is never produced and offer a designer three scope choices out of five. It also mis-set expectations for anyone reading the project's own description of itself. The corrected line states the two passes, the five options, the inclusion-gate filter, and points the assessment variant at `18C`. The `FILE MAP` entry's matching phrase 'two-report architecture' was corrected in the same pass — `00C` already called it the single-report architecture. **THIRD DEFECT FOUND AND FIXED IN THE SAME SWEEP:** ABSOLUTE RULE 2 listed the permitted-inline-style exceptions as 'BLL header lowercase; WJFUN word-highlighter spans' — **two of the four families constraint 2 now documents**, having never been updated when Assessment Mode added its two (`margin-bottom` on a content table, `lower-alpha` on a nested `<ol>`, `18` §6.4). Rather than re-list them and go stale again, the rule now names none and points at constraint 2 as the authority — the same anti-drift shape used in `19_SKILLS`. **HOW THE ADDITION WAS PAID FOR (`CLAUDE.md` §2):** the file cannot be split, so it must stay under the 30,000-byte soft limit. ABSOLUTE RULE 5 was restating, word for word, the four red-note prefixes, the never-disclose-an-answer rule and the whitelisted-reviewer rendering rule that CORE PHILOSOPHY already defines **in full, one screen above in the same file**. It now keeps the prohibition and the four prefix names and points up to CORE PHILOSOPHY for the definitions. **No rule was weakened, narrowed or removed** — every sentence cut is still stated, in full, in the same document. Net result **29,992 → 29,987 bytes**. | (c) Universal — no scope named, so universal per constraint 86; it changes no conversion output in any case | `_project_instructions_.md` → FILE MAP `09` entry; OUTPUT EXPECTATION → Comparison Mode (the defect); ABSOLUTE RULE 2 (stale exception list → pointer); ABSOLUTE RULE 5 (de-duplicated against CORE PHILOSOPHY, to pay for the addition). **Blast-radius sweep run across every content file for 'Template-Derived', 'three-option', 'two-report' and 'two downloadable': `_project_instructions_.md` was the ONLY carrier.** `09A`, `09B`, `09C`, `00A2`, `00C` and `18C` were all checked and are already correct — `00C`'s file-reference row already says 'the single-report architecture' and lists all five scope options — so **no knowledge file was touched**. **Ledger housekeeping in the same commit (`CLAUDE.md` §4):** appending this row took the open history part past the 30 KB soft limit, so `12E6` was closed at CL-0097 and renamed `12E6_CHANGE_HISTORY_CL0093_0097.md`, this row opened `12E7_CHANGE_HISTORY_CL0098_ONWARD.md` (repeated table header below the sentinel, per the `12C`/`12D` pattern), every `12x` part header was renumbered from 'of 12' to 'of 13' with `12E`/`12F`/`12G` shifted down one, the five closed parts' 'new entries go in …' pointers were re-aimed at `12E7`, and `INDEX.md` was updated for both parts. | Repo housekeeping — direct request from Gavin in a Claude Code session on this repository, 20 September 2026 | Major (mechanism lane — generated HTML byte-identical) | Implemented | Designer's decision (Gavin), 20 September 2026: 'I would really like these to be updated to avoid this misinterpretation ever happening again.' **Part 1 (Locked) checked — nothing blockable:** no CL row governs the system-prompt text's description of Mode 3. **Mechanism lane — no `12G` entry:** generated HTML and CSS are byte-identical. `09` was and remains the authority, so the Convertor's actual behaviour never changed; what changed is that the project's own description of that behaviour now matches it. **Nothing overridden:** the stale wording is **pre-ledger** text that predates the current two-pass architecture and was simply never revised when it landed. **Judgement recorded for confirmation:** cutting ABSOLUTE RULE 5's duplicated sentences was a size decision, not an editorial one — it was the only genuine redundancy in the file large enough to pay for the correction, and the deleted text survives verbatim under CORE PHILOSOPHY. If the designer would rather have the repetition back, the correction needs bytes from somewhere else. **STRUCTURAL PROBLEM RAISED, NOT ACTIONED — this file is now at its ceiling with 13 bytes of headroom.** The 30,000-byte soft limit exists to say 'split this at its next update' (`CLAUDE.md` §2/§4), but `_project_instructions_.md` **cannot be split** — it is pasted whole into the project's Instructions field. The warning is therefore a signal with no possible action, while functioning as a hard ceiling that forces a trim for every future correction. Recommended fix, for the designer to approve: give this one file its own explicit limit in `tools/check_kb.py`, set from what the Instructions field and a sane system prompt actually warrant, instead of a split-me warning it can never satisfy. **`_project_instructions_.md` DOES NOT SYNC FROM THIS REPO** — the designer must re-paste it into the online HTML Convertor project's Instructions field for this correction to take effect. **Still outstanding from CL-0097, unchanged:** `18C` §10.1 says `UPDATE MODE` wins if present while `09A` §1 names only `ADMIN MODE` and the `PAGEFORGE` prefix; that is a rule decision for the designer, not a wording fix, and was deliberately left alone here. |
| CL-0099 | Sunday, 20th September, 2026 | **CORRECTION — claude.ai chat DOES have a `/` skill picker; `19_SKILLS/README.md` and `ROLLOUT.md` said it did not.** Both documents opened on the claim 'Claude.ai has no in-chat skill picker — designers will not choose a mode from a menu', and built the rollout advice on it. **That is false.** The designer demonstrated it: typing `/pa` in a claude.ai project chat opens a picker listing the matching skill by name with its **full description shown alongside**. The claim came from Anthropic's own help pages, which at the time of writing still document the `/` picker as a **Microsoft 365 add-in** feature only ('Type `/` in the sidebar to see available skills and select one') and describe claude.ai chat as automatic-selection-only. The product has moved ahead of the documentation, and the documentation was trusted over the product. **WHAT THIS CHANGES — it is better than what was promised, not worse.** The designer's original requirement was that information about a mode be visible **at the point of choosing it**, falling back to a card shown after the mode starts. The picker satisfies the first, natively: the 200-character `description` is displayed in full next to the name while choosing. So the descriptions are doing double duty — they are what Claude matches on AND what a human reads in the picker — which raises rather than lowers the bar on writing them in plain English. They already were. **The `tekura-` name prefix also stops being merely tidy and becomes functional:** the picker filters as you type, so `/tekura` lists all ten skills together. That prefix was recorded in CL-0097 as a presentational choice offered for renaming; it is now load-bearing and should stay. **Nothing about the skills themselves changes** — no `SKILL.md` was touched. Automatic description-matching still works exactly as before, so the typed trigger phrases (`COMPARISON MODE`, `UPDATE MODE`, and the rest) are unaffected, and the mode card still does its job for anyone who arrives without using the picker. Both documents now describe **two** ways in — the `/` picker and automatic matching — and the rollout's proving test gains a **new step 1: type `/tekura` and expect ten**, because an empty picker is the fastest possible proof that the zips were never uploaded and makes every later test fail for that one reason. | (c) Universal — no scope named, so universal per constraint 86; it changes no conversion output in any case | `19_SKILLS/README.md` → 'Why the typed trigger phrases still work' rewritten to describe both entry paths; `19_SKILLS/ROLLOUT.md` → 'Before you start' point 1 rewritten; the proving test gains step 1 (`/tekura` lists ten) and steps 2-6 renumbered; the designer announcement now tells designers they can type `/tekura` and pick from the list. **No `SKILL.md`, no `dist/` zip and no knowledge file was touched** — the skills' behaviour is unchanged; only the documentation of how a designer reaches them was wrong. | Repo housekeeping — designer-reported error in Claude's own documentation, 20 September 2026 | Routine (mechanism lane — generated HTML byte-identical) | Implemented | Correction of Claude's own error, raised by the designer with a screenshot on 20 September 2026: 'You have said that there is no slash-command picker for skills in claude.ai chat, but then why am I able to do the following???' **Part 1 (Locked) checked — nothing blockable.** **Mechanism lane — no `12G` entry:** generated HTML and CSS are byte-identical; this is documentation of a platform behaviour. **The lesson worth keeping, and the reason this row is detailed:** the help documentation for a fast-moving product surface is evidence, not proof. Two rounds of advice here were shaped by a documented absence that the live product had already contradicted, and the error survived a web-search check **twice** because the search kept returning the same stale page. Where a claim about platform behaviour can be settled by looking at the product, look at the product. A user's screenshot outranks a help article. **Still unresolved, and now the more likely cause of the reported symptom:** the designer typed `/tekura` and saw nothing while `/pa` found `pageforge`. The picker demonstrably works, so an empty result for `tekura` most probably means the ten zips have not been uploaded — pushing this repo does **not** install skills, since the GitHub sync populates project knowledge only. Awaiting the designer's check of `claude.ai/customize/skills`. **Second observation recorded, not actioned:** because `19_SKILLS/` sits inside the synced repo, the ten `SKILL.md` files now also sync into the project's knowledge, spending a little of that budget for no benefit. Harmless but unintended; whether the sync can exclude a folder has not been established. |

---
