> **Last updated:** Thursday, 13th August, 2026
> **Granular part E3 (6 of 9) of `12_CHANGE_LEDGER.md`** — PART 3 change history: CL-0072 to CL-0077. CLOSED — new entries go to `12E4`.
> All sibling parts live in `12_CHANGE_LEDGER/`; see `INDEX.md` at the repo root. Body below is verbatim source-of-truth content.
> *The table header row below is repeated for readability; the rows continue the PART 3 history table. Opened 7 August 2026 when `12E2` reached the 30 KB soft limit, and closed 13 August 2026 when it reached the same limit (`CLAUDE.md` §4).*

<!-- KB-PART-BODY-START -->
| ID | Date | Change summary | Scope | Affected file(s) / rule(s) | Source (intake channel) | Class | Status | Authority decision |
|----|------|----------------|-------|----------------------------|--------|-------|--------|--------------------|
| CL-0072 | Friday, 7th August, 2026 | **PageForge Compare Mode (Mode 7) tightened on three points — the typed command is the ONLY way in, the `{CODE}_interactives.txt` worklist becomes a REQUIRED input, and comments/developer notes are excluded outright.** **(1) NEVER ADVERTISE THE MODE (new).** Not every developer is on the PageForge test programme, so the mode must never be mentioned, offered, suggested or asked about unless the developer types the trigger phrase themselves — **most particularly not at the end of a `COMPARISON MODE` run**, where the artefacts happen to be to hand and the temptation is greatest: no "you can now run PageForge Compare" close, no asking whether they have PageForge files or are testing PageForge, no naming the report for Gavin or the worklist as a next step, and no listing Mode 7 when summarising what the project can do. Nothing else starts it either — not PageForge-looking files, not a `cv2-int-ref` box spotted in an upload, not a worklist arriving on its own, not the tester having just finished Mode 3. The sole exception is a developer who raises PageForge first (types the phrase, asks about the mode directly, or uploads files they themselves describe as PageForge output). The `PAGEFORGE`-prefix discriminator already carried in `09A` is explicitly re-scoped as an **accident guard, not a licence to raise it**. **(2) THE WORKLIST IS NOW REQUIRED (was optional).** Developers upload `{CODE}_interactives.txt` alongside the PageForge pages, because a tester's PageForge files may well have been through Mode 6 and the Page Stitcher — at which point the pages alone no longer record what PageForge itself did. The worklist does three jobs no other artefact can: **(a) who built each widget** (it lists the un-built interactives only, so a finished widget matching an entry's position was stitched by Claude, and one matching no entry was built by PageForge); **(b) THE BOUNDARY EVIDENCE — the reason Chris asked for it** — each entry's **`Activity:`** line names the activity box PageForge assigned the interactive to (compare it with the activity the WRITER put it in: a mismatch is a class B boundary finding visible from the worklist alone) and its **`Content:`** block is verbatim the writer content PageForge pulled into the widget, i.e. the boundary itself, which survives after stitching has removed every `cv2-*` trace from the page; **(c) inventory reconciliation** against the worklist header's `TOTAL INTERACTIVES` / `BUILT AUTOMATICALLY this run` / `STILL UN-BUILT` / `FILES:` counts, with any mismatch stated in the report rather than quietly resolved. Ask for the file **by name** when it is missing; if the tester confirms it is genuinely unavailable, run with the documented limits (finished-markup interactives marked `state unknown`, no class C finding, boundary unverifiable for those) and state the limits in the report header and the chat. It remains **evidence, never a Mode 6 work order**. **(3) COMMENTS AND DEVELOPER NOTES EXCLUDED OUTRIGHT — parity with Mode 3.** `16B` §5.4 is rewritten from "PageForge's own designer-facing notes" to the full Mode 3 rule (`09B` Exclusion 1): never report, **in either output and in either direction, added or removed**, PageForge's red notes (`Writers Note:` / `Red Flag:` / `Designer/Developer To Do:` / `Note from {author}:`), a note the developer ADDED with no PageForge counterpart (including one addressed to the course writer), a note reworded/relabelled/restyled/re-prefixed, an HTML comment (`<!-- … -->`) present in one file and not the other, a restated writer instruction, or the presence/absence/position/ordering of any of these. Rationale, in Chris's words: these are updated, added and deleted as the writer and developer convey module-development information to each other, are separate from the actual content, and are therefore to be completely ignored when the report for Gavin is generated. **Two carve-outs preserved:** a writer `[tag]` or the writer's own content **leaking onto the page as visible text** is still a class A/D finding (the test is whether the text is addressed to a developer or is the writer's material rendered badly); and where a note is what appears to have closed an activity early, the **boundary** is still reported — described in terms of the content that landed in the wrong container, never the note's wording. The report's exclusion counter is renamed accordingly (`comments, developer notes & restated writer instructions`) and the report header's "what it deliberately leaves out" block states the exclusion explicitly so Gavin reads the findings with the right scope | (c) Universal — refinement of Mode 7 (CL-0071) | `16A` (§1 trigger — typed-phrase-only; **NEW §1.3 never-advertise**; §2 step 5; **§4 three-inputs-plus-optional → FOUR REQUIRED INPUTS**; §4.3 rewritten — the three jobs + the missing-worklist limits; §5.3 table row; §6 workflow steps 0/1/5), `16B` (§3.2 step 3 — read a stitched widget's boundary from the worklist, and check the `Activity:` line on its own account; **§5.4 rewritten**; §5.3), `16C` (header "what it leaves out" block + the state-unknown note; §5 exclusion counter renamed), `09A` (**NEW prohibition on ever raising Mode 7 during a Comparison Mode run**), `00A` (Mode 7 section + triage bullet), `00C` (file reference row), `00F` (when-to-load row), `00G` (**NEW constraint 81**), `_project_instructions_.md` (triage bullet, file map), `INDEX.md` | Direct-typed designer instruction (Chris), 7 August 2026, with `OSAI501_interactives.txt` supplied as the worked example of the worklist's shape | Routine | Implemented | Designer decisions (Chris, 7 August 2026): (i) the mode is command-triggered only and is never surfaced to a developer who has not asked for it, because only some developers are testing PageForge; (ii) the worklist is uploaded on every run, so that the correct boundaries imposed on interactives AND activities can be checked even when the interactives have been separately built by Claude and stitched in; (iii) comments/developer notes/writers instructions are ignored exactly as they are in the ordinary Comparison Mode. No conflict with CL-0071 (this refines it) or with any locked decision; not escalated. Cost: `16A` 17→20 KB, `16B` 19→21 KB, `09A` 15→16 KB, `00G` 10→13 KB — all well inside the 30 KB soft limit; the only always-in-context cost is ~0.9 KB on `_project_instructions_.md` |
| CL-0073 | Monday, 10th August, 2026 | **Taonga (The Arts): tui illustrations DELIVERED — the asset convention becomes per-phase folders with multiple poses.** The design team has uploaded the tui artwork into the refresh template and updated both the Taonga global-parameters document and the template's central-images snippet file. Two changes follow. **(1) THE PATH CHANGES — a folder per phase, several poses per folder.** The flat one-image-per-phase convention recorded by CL-0034 (`tui_characters/tui_phase_1.jpg` … `tui_phase_4.jpg`) is replaced by **`tui_characters/phase_{N}/tui_{P}.jpg`**: four phase folders `phase_1`–`phase_4` keyed as before to the module's phase, each holding **poses 1–6**, except **`phase_4`, which holds only 4 poses**. Supplied markup (phase 1, pose 1): `<img class="img-fluid imageCentral" loading="lazy" src="tui_characters/phase_1/tui_1.jpg" alt="Tui character" >` — class, `loading`, and `alt` all unchanged from CL-0034. The design team's snippet file `Z:\Development\Online\Templates\Refresh Template - modules\refresh_template_HTML\tools snippets\refresh_imageCentral.html` is recorded as the reference carrying the full worked examples. **(2) THE ART DEFERRAL IS LIFTED.** The tui is no longer "under development": CL-0017 deferred it as a placeholder, CL-0034 documented the markup while keeping the deferral, and the artwork now exists — so the standing `Designer/Developer To Do:` note *"Tui character art still under development"* is **retired** and the `<img>` is emitted as a real asset reference. **What the source does NOT settle, handled by the house pattern rather than invented: which pose belongs at a given placement.** Nothing in the writer's template names a pose and the design team has not stated a selection rule, so the scaffold emits **`tui_1.jpg`** (the pose used in the supplied example) and carries a visible `Designer/Developer To Do:` note that the pose is a designer choice — the CL-0013 deferred-item pattern, not a derived rule. Two guards: the **phase folder stays derived** from the module's phase, with an unstated phase still a `Red Flag:` (CL-0034, unchanged); and a pose **above the phase's ceiling** (above 6, or above 4 in `phase_4`) does not exist — `Red Flag:` rather than a path to a missing file. **Supersedes the path and deferral limbs of CL-0034**; CL-0034's derived-phase rule and supplied attributes survive, restated here | Cohort — Taonga modules (all phases) | `14A` §14.3 (Tui bullet rewritten — per-phase folders, pose range, pose-choice To Do, ceiling `Red Flag:`, snippet reference; art deferral removed); `00C` → FILE REFERENCE INDEX (§14 Taonga fragment); `12C` (CL-0034 status annotated `superseded by CL-0073`); `INDEX.md` | Direct-typed (Update Mode) — designer message + `Taonga Global Parameters.docx` (Chris, 10 August 2026) | Major | Implemented | Designer decision (Chris, 10 August 2026): *"I've just uploaded some new content into the refresh template (some tui illustrations), this has been updated in the global params document … and the refresh template, specifically the centralImages file … and ready for the claude ai to update with it."* Conflict check clean — **Part 1 (Locked) is empty**, so nothing blockable; the prior decisions on this rule (CL-0017, CL-0034) are both **unlocked and direct-typed**, so a direct-typed supersession is actioned without escalation and without pausing (`11A` §6 — report-vs-report is the only escalation trigger, and neither side here is a report). Classified **Major** per `11A` §5 (a look-and-feel/asset change standing across a whole cohort), consistent with CL-0034. The pose-selection gap is recorded as a deferred designer choice rather than resolved by invention — flagged in the run report so the design team can supply a selection rule if one exists. Cost: `14A` 21→22 KB, well inside the 30 KB soft limit; no always-in-context cost (`_project_instructions_.md` carries no tui detail and is untouched, so no re-paste is needed) |
| CL-0074 | Thursday, 13th August, 2026 | **Interactives Build Mode (Mode 6) now emits ALL of its built-interactives files in ONE response — the one-file-per-response cadence is retired for this mode.** Previously a 20-interactive worklist announced a 3-file plan and then delivered file 1 only, ending "Say **next** for file 2", so the developer had to prompt twice more to get a complete hand-off. That pacing served no purpose here: the Page Stitcher accepts any number of built files in a single upload, so the split costs the developer nothing to receive but three round-trips to obtain. Now the plan is announced and **every planned file follows in that same reply**, back to back. **The ≤8-builds-per-file cap and the early-close judgement rule are UNCHANGED** — splitting stays a **file-size safeguard**, never a pacing device — as are the anchor contract, the quality gate and the file skeleton. The only permitted second response is a **forced continuation** when a run genuinely exceeds a single reply's length: finish the current file at its last complete `</section>`, name the remaining codes, and **carry on unprompted** — it is a limit being hit, not a cadence. **Explicitly scoped to Mode 6: Split Mode (Mode 5) is untouched** and keeps its deliberate one-file-per-response emission under `CL-0011`; a scoping note added to `13` §5A states this in both directions so the two are never conflated again (a Split Mode section file is one slot of a round-trip the designer reassembles turn by turn; a built-interactives file is an independent batch the Stitcher consumes wholesale) | (c) Universal — Mode 6 only | `15A` (§6 rewritten — new "EMIT EVERY FILE IN ONE RESPONSE" sub-section, plan example de-cadenced, Mode 5 scoping note; §10 tone; part banner), `00A` (Mode 6 section), `00C` (file reference row), `00G` (**constraint 78** — output paragraph rewritten), `13_SPLIT_MODE.md` (**new §5A scoping guard** — cadence is Split Mode only, Mode 6 does the opposite), `_project_instructions_.md` (Mode 6 triage bullet, file map, output-shapes bullet), `INDEX.md` | Direct-typed designer instruction (Chris, 13 August 2026) | Major | Implemented | Designer decision (Chris, 13 August 2026): *"I want ALL of the files to be generated at once to save the user having to manually prompt Claude to keep generating one file after another until all of the necessary files are generated"*, and — asked directly whether the change should extend to Split Mode — *"Apply ONLY to the interactives worklist mode please"*, plus *"Keep the cap"* on the ≤8 split. Conflict check: **Part 1 (Locked) is empty**, so nothing blockable. `CL-0011` **does** cover a one-file-per-response cadence but is **Split Mode only** and is left in force untouched — no conflict, and the new `13` §5A guard hardens that boundary rather than crossing it. No prior ledger row governs Mode 6's emission cadence (`CL-0071`/`CL-0072` concern Mode 7). Classified **Major** per `11A` §5 (universal scope within a mode, changes a documented output behaviour) |
| CL-0075 | Thursday, 13th August, 2026 | **NEW constraint 82 — the opening `<body class="…">` tag is mandatory on every generated page.** Delivered modules have been observed with the `<body>` tag simply absent: `</head>` runs straight into `<div id="header">`. This is a **hard fault, not cosmetic** — the browser opens an implicit `<body>` carrying **no class**, so every rule hanging off `container-fluid` / `fundamentals` / `inquiry` / `reoTranslate` silently stops applying and the page renders wrongly while the source still looks plausible, which is why it survives review. The knowledge base already covered the *inheritance* half of this (`06` §4.2 — MXFU401 and XTAS101 are known reference modules missing the tag, "never carry it into new modules") but nothing said the **output itself** must always carry it. Constraint 82 states the full document shell as an ordered, all-present list (`<!doctype html>` · `<html template>` · `<head>` · **`<body class>`** · `#header` · `#body` · `#footer` · closers), fixes the class as **derived, never guessed** from the sub-type per `06`'s Quick Identifier Table, and requires the Standard `container-fluid` default **plus a visible `Designer/Developer To Do:` note** where a sub-type genuinely cannot be resolved — never a class-less `<body>`, never none. **Two carve-outs, both pre-existing and deliberate:** Interactives Build Mode fragments (constraint 78 — the section starts at the widget's own wrapper) and Split Mode **section** files (only the base file carries the shell) | (c) Universal | `00G` (**NEW constraint 82**), `06_TEMPLATE_RECOGNITION.md` (§4.2 — new mandatory-output warning block distinguishing "don't inherit it" from "don't produce it"; §5 Validation Checklist — the reference-file check split from a new my-output check), `02C` (Template & Structure — new `<body class>`-present line + a shell-balance line), `INDEX.md` | Direct-typed designer report (Chris, 13 August 2026) | Major | Implemented | Designer report (Chris, 13 August 2026): *"I have noticed in a couple of modules that the body tag is missing after the header. `<body class="container-fluid">`"*. Conflict check clean — **Part 1 (Locked) is empty**; no prior row governs the document shell's completeness, and the closest neighbour (`06` §4.2's inheritance rule) is **extended, not contradicted**. Classified **Major** per `11A` §5 (universal, structural, and it introduces a new numbered constraint). The two fragment carve-outs were checked against constraint 78 and `13` before drafting so the new rule cannot be read as requiring a shell where those modes correctly omit one |
| CL-0076 | Thursday, 13th August, 2026 | **SCES302 Difference 1 — the superseded lesson-`<h1>` wording is swept out, and a settled universal constraint is closed off as a pre-flight question. NO RULE REWRITE: constraint 79 was already correct.** SCES302 shipped all eight lesson pages with the MODULE title in the header. Constraint 79 (and constraint 16, and `01A` → Lesson Page Header) already say the lesson page's `<h1><span>` carries **that lesson's own** title; the failure was **upstream of the rule** — the retired wording it superseded ("lesson `<h1>` = MODULE title; first `Lesson N` H2 demoted to a body `<h3>`") was still reachable in the knowledge base, was surfaced during conversion and read as live, and was then put to the designer as a pre-flight choice ("module title (constraint 16)" vs "lesson title (match SCES201)"). The designer answered "module title" and the conversion followed it, against both constraint 79 and the reference module's own files. **Two fixes, no rule text changed. (1) THE SWEEP.** Every surviving trace of the retired phrasing is corrected or unmistakably marked retired, so a search cannot surface it as guidance: `02C` line 54 (**a live contradiction** — it required *dual* `<h1><span>` titles on EVERY Years 9–10 / NCEA lesson page; now a single lesson-scoped title, with a second only where the writer named that lesson bilingually), `02C` line 145 (OSSC-legacy framing → "all series, all year levels"), and **`07D` line 336 — an un-swept MTK contradiction** still asserting module titles in `<h1><span>` "on both overview and lesson pages" (now overview = module titles, lesson = that lesson's own title, constraint 79 applying to MTK too). Constraint 16 and the superseded constraint 69 already carry explicit retirement markers and are left as the audit record. **(2) THE PRE-FLIGHT LOOPHOLE.** A new block in `00B`'s pipeline preamble states that the structural reference and the image output mode are **the only two** pre-flight questions, that a settled `(Universal)` constraint is **never** offered as a choice and a superseded rule is **never** a live alternative, and that a designer instruction contradicting a universal constraint is **flagged back and confirmed, never followed silently** (a confirmed override being a Mode 4 change, not a one-off answer). Constraint 79 gains a matching "NOT A PRE-FLIGHT QUESTION" sentence, and `11A` §8 gains items **6** (same rule for Update Mode) and **7** (**superseding a rule is not finished until the retired wording is swept** — part of the §9 blast-radius sweep, not optional tidy-up) | (c) Universal | `02C` (lines 54, 145), `07D` (line 336), `00B` (**new pre-flight scope block**), `00G` (constraint 79 — pre-flight sentence added), `11A` §8 (**new items 6 and 7**) — *which pushed `11A` past the 30 KB soft limit, so §8-9 were split verbatim into a new `11C_UPDATE_MODE_PREFLIGHT_SWEEP.md` per `CLAUDE.md` §4*, `INDEX.md` | Finalized difference report — SCES302, Difference 1, scope (c), 10 August 2026 | Major | Implemented | Designer scope decision (c) Universal, with the report's own actioning note directing that **no rule be rewritten** because constraint 79 already states the correct behaviour. Conflict check clean — **Part 1 (Locked) is empty**, and this **reinforces** `CL-0069` (which created constraint 79) rather than contradicting it. **The report's item 3 — "cross-check against the open FRNO901 constraint-79 patch" — is DISCHARGED as not applicable:** a repo-wide search finds **zero** occurrences of `FRNO901` in any part file or ledger row, so no such open item exists here; if one is being tracked outside this repository it is unaffected by this change |
| CL-0077 | Thursday, 13th August, 2026 | **SCES302 Difference 3 — constraint 63 OVERTURNED: the activity's inner text column is `col-12`, and the two-row split survives only in a WIDENED wrapper.** Two linked corrections. **(a) The inner text column is `col-12`, never `col-md-8 col-12`** — the activity wrapper already sets the reading width, so an inner `col-md-8` narrowed the prose a second time. **(b) The split is now width-conditional:** at the **DEFAULT** wrapper width (`col-md-8 col-12`) the heading, instructions and the interactive share **ONE** inner `row` > `col-12`, because there is no width difference to express; the **two-row split is retained ONLY where the wrapper is widened** (`col-12` Standard / `col-md-11 col-12` Inquiry & Fundamentals / `col-md-12 col-12` full width), where the text sits in its own row and the interactive follows in a separate row at the wrapper's own width. A text-only activity still needs no split at either width. **Supersedes the "always separate rows, prose stays at `col-md-8`" rule** established by `CL-0036` (widened wrappers) and generalised to every activity by `CL-0048`. **Constraint 56 (wrapper widths) is unaffected** — this changes only what happens *inside* the wrapper | (c) Universal | `00E` (**constraint 63 rewritten**), `05B` → COMP_14 → Activities (rule text, **both** worked examples — the default one-row shape and the widened split — **and** the file's own canonical Activity Structure block at the top, whose 1B example still taught the overturned pattern), `04A` → COMP_07 (the in-activity accordion example's inner column), `02B` (rule text + both patterns), `02C` (the single checklist line replaced by three — `col-12` column, default = one row, widened = split), `02D` (internal constraint 31 `wide_interactive_containers`), `00B` (Pipeline Phase 6 line), `00G` (constraint 77 — aligned to name `col-12` as the activity's inner column) — *the `05B` edits brought that file to within 107 bytes of the 30 KB soft limit, so COMP_14 was split at `## Buttons` into a new `05D_COMP14_BUTTONS_TABLES_COLUMNS.md` per `CLAUDE.md` §4, with the `05A`/`05C` part banners bumped to "of 4"*, `INDEX.md` | Finalized difference report — SCES302, Difference 3, scope (c), 10 August 2026 | Major | Implemented | Designer scope decision (c) Universal. Conflict check: **Part 1 (Locked) is empty** — nothing blockable. The two prior decisions on this rule, **`CL-0036` and `CL-0048`, are both `Implemented` and UNLOCKED**, and both arrived **direct-typed**, so a supersession arriving via a finalized difference report is actioned without escalation (`11A` §6 — report-vs-report is the only escalation trigger, and neither prior side is a report). Both rows should be read with this supersession. **Recorded caveat for a future check:** the report evidences the shared-row shape in code at the DEFAULT width (activity 3B) and names 3C/5C as retaining the split, but does not show the widened case's own text-column width in code; the rule as written applies `col-12` to the inner text column at **both** widths, per the report's flat statement that "the activity's inner text column is `col-12`". If a widened activity later proves to hold its prose at a different width, that is the line to revisit |
