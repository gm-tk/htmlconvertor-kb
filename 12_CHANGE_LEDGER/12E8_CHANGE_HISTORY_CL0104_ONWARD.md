> **Last updated:** Tuesday, 22nd September, 2026 4:52 PM
> **Granular part E8 (11 of 14) of `12_CHANGE_LEDGER.md`** — PART 3 change history: CL-0104 onward (continued) — THE OPEN PART: APPEND NEW ENTRIES HERE.
> All sibling parts live in `12_CHANGE_LEDGER/`; see `INDEX.md` at the repo root. Body below is verbatim source-of-truth content.
> *The table header row below is repeated for readability; the rows continue the PART 3 history table. Opened 22 September 2026 when `12E7` reached the 30 KB soft limit and was closed at CL-0103 (`CLAUDE.md` §4).*

<!-- KB-PART-BODY-START -->
| ID | Date | Change summary | Scope | Affected file(s) / rule(s) | Source (intake channel) | Class | Status | Authority decision |
|----|------|----------------|-------|----------------------------|--------|-------|--------|--------------------|
| CL-0104 | Tuesday, 22nd September, 2026 | **CONTENT DECIDES THE MODE — the Conversion Mode trigger no longer depends on what the developer types.** The triage read *'Content source uploaded/pasted **+ request for HTML** → Conversion Mode'*, and the ambiguity fall-back was scoped to a `.docx`. A bare parsed-`.txt` upload with nothing typed therefore matched **neither** line — it worked only by inference. **EVIDENCE:** a survey of ~100 project conversations back to 20 July, 41 opening messages read verbatim (report supplied by the designer, 22 September 2026). **Half the openings carry no instruction at all** — 9 of 41 blank, 3 a bare module code, 4 an image-mode line only. When developers do type, it is frequently a stale paste: three chats asked to *'complete JPFUN01 lesson 0.0'* while uploading WJFUN212, WJFUN305 and WJFUN306, and every one of them built the whole module. The image-mode line appears in two-thirds of openings and is habit, not information. **The files were doing all the work, every time.** The cost was measured too: in SCBI202, with template, iStock file and reference pages all attached, the Convertor asked *'What would you like me to do with these files?'* and offered *'fix or complete the SCBI301 pages'* as an option. **THE RULE IS WRITTEN AS PRINCIPLES, NOT AS A LIST OF OBSERVED CASES** — the designer's explicit instruction, so that variations the survey never saw are covered too. **(1) Identity comes from content, never from labels.** The survey found filenames routinely wrong — `JFUN111` for WJFUN111, `PW0100` for PWY1003, a file named `ENFUN05` that was PWY1006, `PW1003` metadata inside PWY1007 — so **filenames decide nothing**, and where filename, metadata and the template's own tags disagree on the module code **the tags win and the mismatch is flagged, never asked about**. **(2) An unconverted writer's template IS the trigger**, whatever was typed — a request, a bare code, an image-mode line, a stale instruction naming another module, or nothing. **(3) Media List, iStock acks and sibling HTML are supporting inputs**: each was absent from real conversions (BLLR206 without acks, FRNO901 and WJFUN210 without any reference, ARTP406 with an empty Media List), so none can be required; only a **structural reference** is still asked for when none is supplied. **(4) No writer's template = not a conversion** — one test covering every don't-fire case, including ENWJ106 where the template simply failed to attach and the right answer was to say so. **(5) Anything that identifies itself outranks inference** — which is what protects the survey's strongest near-miss, SCCH302 (30 July): a `.txt` plus nine `.html` pages that looked exactly like template-plus-references, but the `.txt` was an interactives worklist and the pages carried `cv2-int-ref` boxes. **(6) It fires on the FIRST conversion in a chat only** — later uploads were nearly always comparisons or edits. | (c) Universal — no scope named, so universal per constraint 86 | `00_MASTER_INSTRUCTIONS/00A2_OPERATING_MODES.md` → Mode triage: the conversion fall-through replaced by the two principle bullets, and the ambiguity line re-scoped from '`.docx` with no clear instruction' to 'a file whose **content** does not identify it'; `_project_instructions_.md` → the same two changes in compressed form (+1,275 bytes, which only fits because CL-0103 lifted this file's ceiling in the commit immediately before). **Blast-radius sweep run** for every other statement of the conversion trigger across all content files: there is none — `00F` has no Conversion row (it maps the explicit triggers only) and the two `uploaded/pasted` hits are the interactives worklist, which the new text restates consistently. No COMP file, no pipeline, no constraint touched. **Ledger housekeeping in the same commit (`CLAUDE.md` §4):** this row took the open history part past 30 KB, so `12E7` was closed at CL-0103 and renamed `12E7_CHANGE_HISTORY_CL0098_0103.md`, this row opened `12E8_CHANGE_HISTORY_CL0104_ONWARD.md`, every `12x` header was renumbered 'of 13' → 'of 14' with `12E`/`12F`/`12G` shifted down one, the closed parts' pointers re-aimed, and `INDEX.md` updated for both. | Finalized survey of the project's own conversation history, commissioned by Gavin and supplied 22 September 2026 (~100 chats to 20 July; 41 openings read verbatim) | Major (mechanism lane — generated HTML byte-identical) | Implemented | Designer's decision (Gavin), 22 September 2026, on evidence he commissioned from the project's own chat history after this gap was raised. **Part 1 (Locked) checked — nothing blockable:** no CL row governs the conversion trigger. **Mechanism lane — no `12G` entry:** generated HTML and CSS are byte-identical; what changes is which mode a message reaches, not what the mode produces. **Nothing overridden:** the '+ request for HTML' wording is **pre-ledger** text, narrowed rather than reversed — every message that used to trigger a conversion still does. **Judgement recorded for confirmation — two recommendations from the survey were deliberately NOT actioned here.** The report recommends defaulting image mode silently to Direct Link (virtually every chat used it; BLL247 the lone exception); that contradicts **constraint 9**, which requires prompting when the user has not stated one, so it is a constraint amendment in its own right and is left for the designer. It also leaves two questions that genuinely cannot come from the files — which sibling set to follow when several disagree (BLLR204 had two that contradicted each other), and split-versus-single — both of which remain questions, the latter already covered by `13`'s proactive-offer rule. **`_project_instructions_.md` does not sync from this repo** — it must be re-pasted into the online project's Instructions field before any of this takes effect in a conversation. |
| CL-0105 | Tuesday, 22nd September, 2026 | **The two PageForge skills' descriptions rewritten, both now opening `!!!PAGEFORGE TESTING ONLY!!!`** — an audience warning rather than a mode label, which is right for these two: only some developers test PageForge, and `16A` §1 / constraint 80 forbid the Convertor ever raising Mode 7 unprompted. `pageforge-compare-mode` → '… Compare PageForge's generated HTML against the developer's finished HTML and report its differences to Gavin for refining the module conversion process.' (181 chars). `pageforge-interactives-mode` → '… Build the un-built complex interactives listed in a PageForge {CODE}_interactives.txt file that is later stitched back into the original HTML files.' (177 chars). **'Complex' is the KB's own term and is used correctly:** constraint 80 defines non-complex as a type PageForge already has a builder for, so the interactives left un-built are the complex ones. The one edge the word does not cover is a **non-complex** type left un-built — which is a PageForge fault, reported as a Mode 7 class C finding, and still built by Mode 6 when it appears on a worklist. **TRIGGER-PHRASE CONSEQUENCE, RAISED NOT CORRECTED — and sharper for Mode 7 than for any earlier case.** Neither description now contains its typed phrase. For most modes that only weakens automatic matching; for **PageForge Compare it very likely means the skill never fires from typing at all**, because `16A` §1 makes the typed phrase the *only* way in — the mode is never offered, never advertised, never inferred. So Mode 7's skill is now effectively picker-only, and the mode card will not appear for a developer who types `PAGEFORGE COMPARE MODE`. **The mode itself is unaffected:** `00A2` → Mode triage and `_project_instructions_.md` check the `PAGEFORGE` prefix ahead of `COMPARISON MODE` independently of any skill, so the run routes correctly either way. Mode 6 loses less, because a worklist upload is self-identifying and is the usual route in. **Measured for the designer:** re-prefixing `PAGEFORGE COMPARE MODE - ` would total 206 characters (6 over), `PAGEFORGE COMPARE - ` 201 (1 over); `INTERACTIVES MODE - ` would fit at 197. None applied — the designer's wording stands. | (c) Universal — no scope named, so universal per constraint 86; it changes no conversion output in any case | `19_SKILLS/pageforge-compare-mode/SKILL.md` and `19_SKILLS/pageforge-interactives-mode/SKILL.md` — `description:` frontmatter only; both zips rebuilt. No skill body, guard, mode card, precedence rule or knowledge file touched. | Repo housekeeping — direct request from Gavin in a Claude Code session on this repository, 22 September 2026 | Routine (mechanism lane — generated HTML byte-identical) | Implemented | Designer's decision (Gavin), 22 September 2026, wording supplied verbatim and applied character-for-character. **Part 1 (Locked) checked — nothing blockable.** **Mechanism lane — no `12G` entry:** generated HTML and CSS are byte-identical. **The trend recorded across CL-0101, CL-0102 and this row is now at its end point for these two skills:** as the descriptions are rewritten for the person reading the picker, they have shed their trigger phrases one by one. That has been the right trade each time — 200 characters can do one job well, and the picker is the primary route — but the belt-and-braces CL-0096 built is now gone for Mode 7 specifically. Worth a deliberate decision rather than further drift: either accept that the typed phrases are served by the KB triage alone (which works, and always has), or reserve the opening words of each description for its phrase and write the human explanation in what remains. |

---
