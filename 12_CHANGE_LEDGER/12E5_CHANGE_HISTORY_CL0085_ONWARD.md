> **Last updated:** Friday, 28th August, 2026 2:54 PM
> **Granular part E5 (8 of 11) of `12_CHANGE_LEDGER.md`** — PART 3 change history: CL-0085 onward (continued) — THE OPEN PART: APPEND NEW ENTRIES HERE.
> All sibling parts live in `12_CHANGE_LEDGER/`; see `INDEX.md` at the repo root. Body below is verbatim source-of-truth content.
> *The table header row below is repeated for readability; the rows continue the PART 3 history table. Opened 26 August 2026 when `12E4` reached the 30 KB soft limit and was closed at CL-0084 (`CLAUDE.md` §4).*

<!-- KB-PART-BODY-START -->
| ID | Date | Change summary | Scope | Affected file(s) / rule(s) | Source (intake channel) | Class | Status | Authority decision |
|----|------|----------------|-------|----------------------------|--------|-------|--------|--------------------|
| CL-0085 | Wednesday, 26th August, 2026 | **NEW constraint 85 — a THREE-part `[TITLE BAR]` builds THREE `<h1><span>` titles, in the Languages cohort ONLY.** Languages writers give the module title in three languages, `|`-separated — `[TITLE BAR] HALLO, DEUTSCH! | HELLO, GERMAN! | NAU MAI KI TE REO TIAMANA!` (`GENO901`) — but the KB's only title rule was the two-title English / Te Reo split, so the conversion merged the first two parts into one heading (`Begrüßung und Vorstellung | Welcome and Introduction`). **THE RULE:** for a Languages-cohort module (Chinese, French, Gagana Samoan, German, Japanese, Spanish) each `|`-separated part of a three-part title bar becomes its **own** `<h1><span>` on the overview page, **in the writer's order** — the cohort uses both orders (`GENO901`, `GEFUN01`, `SPA1004` and the Samoan `1005 Sasa` lead with the target language; `GEFUN06` leads with English), so the conversion never re-orders. An empty part, or an unfilled placeholder (`TE REO`, `MODULE TITLE TE REO`, `INSERT MODULE TITLE TE REO`), is **skipped**; fewer than two real titles after skipping → `Red Flag:`. **SCOPED DELIBERATELY — the universal form was measured and REJECTED.** The change was requested universally (split on every `|`). Pre-change measurement over the whole connected corpus — **3,981 `.docx` scanned, all 1,472 `[TITLE BAR]` lines** (941 no pipe, 513 one pipe, **11 modules** with two pipes) — found only **5** of the 11 genuinely trilingual, **every one Languages-cohort**; the human-built HTML for the other **6** ships **two** headings: `ANZH304` keeps the pipe *inside* the heading (`<h1><span>Mana Motuhake | Te Tiriti o Waitangi</span></h1>`), `XGF9003` joins an English title + English subtitle with an en-dash, `HIS1007` / `HIS1008` drop a second title pair, and `TEFUN01` / `TEFUN04` drop the unfilled `TE REO` placeholder. A `|` legitimately survives inside a finished `<h1>` in three modules (`ANZH304`; `HIS1001` — `Independence | Colonisation | Independence`; `XMES201` — `My Pets | Aku Mokai`, six pages), and **no** page in the 2,385-page finalized corpus carries a genuine three-language title (all 37 three-`<h1>` pages are artefacts — `Lorem Ipsum` template residue, `Maori title not provided` notes, or duplicates). The universal form would have regressed 6 modules to gain 5. Outside the cohort the two-title rule is unchanged and a two-pipe title bar emits the pair plus a visible `Red Flag:` quoting the original. **Order left as the writer's** — the finalized corpus does re-order the *two*-title case to English-first (`TEFUN03`), but there is no built precedent for the three-title case, so no re-ordering is invented; raised with the designer and deferred. | (b) Series-scoped — Languages cohort (Chinese, French, Gagana Samoan, German, Japanese, Spanish) | `01A` → Template Title Patterns → **TITLE BAR PARSING RULE** (new “THREE-PART `[TITLE BAR]` — LANGUAGES COHORT ONLY” block + the non-Languages guard); `14A` → § 14.1 Languages Phase 1–4 (new three-part title bullet); `00G` → **NEW constraint 85**; `02C` → Headings & Titles checklist (two new lines); `02D` → policy item 17 `always_split_dual_titles` | Direct-typed (Update Mode) — designer request, 26 August 2026 (Chris); scoped after pre-change corpus measurement | Major | Implemented | Designer decision (Chris) — approved the scoped Languages-only form after the measurement showed the universal form would break 6 modules. **Part 1 (Locked) is EMPTY — nothing blockable.** No prior CL row covers three-part title bars; `CL-0069` / `CL-0076` (constraint 79, lesson-page titles) are untouched. |
| CL-0086 | Friday, 28th August, 2026 | **NEW Mode 8 — `ADMIN MODE`, and NEW constraint 86.** Update Mode's caution is correct for a **designer's** proposed change (it asks for a missing scope, pauses before overturning a prior decision, blocks on a lock, escalates report-vs-report disagreements to Persephone) but wrong for a change that **arrives already approved**. **THE RULE:** the phrase **`ADMIN MODE`** (case-insensitive) is the project's **highest-precedence trigger** — checked before `PAGEFORGE COMPARE MODE`, `COMPARISON MODE`, `UPDATE MODE`, `SPLIT MODE` and `INTERACTIVES MODE`; a message carrying `ADMIN MODE` **and** another trigger is an Admin Mode run. It marks a change from **an authorised individual — Gavin (who builds and owns these instruction files) or the lead designer**. Typing the phrase IS the assertion of authority: the Convertor never asks who is speaking, never asks for approval, **never asks for a missing scope** (an unscoped Admin Mode change defaults to **(c) Universal** — the one deliberate exception to `11` §3's never-default rule — unless the message names a subject, series, module code prefix, year level or documented cohort), never pauses to confirm a guardrail change, and **never escalates** (the Section 7.1 Persephone catalog is never produced in this mode). **It overrides any conflicting decision on record regardless of source AND regardless of lock state** — a `Locked` row is superseded with **no unlock step** and leaves Part 1 — and the **newest** Admin Mode instruction always wins over an earlier one. **No override is silent:** the run states what it displaced and the superseded row is written `Reverted — superseded by [new ID] (ADMIN MODE)`. Every Admin Mode change is ledgered `Source = ADMIN MODE (authorised)`, `Status = Locked (admin)` (a NEW status), written to Part 1 and the Part 3 history; a `Locked (admin)` row **blocks** a later ordinary Update Mode change and is released only by another Admin Mode message, never by a Persephone unlock. **Everything about HOW edits are made is unchanged** — same any-format intake, same blast-radius sweep, same retired-wording sweep, same single **Repo Update Brief** for Claude Code on `htmlconvertor-kb`, same repo ritual, same `check_kb.py` guard, same never-regenerate rule. The **only** question Admin Mode may still ask is one of **comprehension** — an instruction that is unintelligible, targets nothing that exists, or contradicts itself — never one of approval. **Update Mode is untouched for everything it already did:** a designer's finalized difference report still runs through Mode 4 with its scope questions, conflict pause, lock block and Persephone escalation intact. *(Designer-flagged inference, recorded deliberately: Chris specified admin-over-update and admin-over-admin precedence; the treatment of a LATER ordinary Update Mode change that contradicts a prior Admin decision — blocked as `Locked (admin)` — mirrors the existing lock mechanic and was chosen as the conservative, reversible reading. Raised with Chris in the run summary.)* | (c) Universal | **NEW `17_ADMIN_MODE.md`** (whole file); `00A` → OPERATING MODES (seven → eight modes; new Mode 8 section; new first triage bullet); `00F` → WHEN TO LOAD (two new rows); **`00H` → NEW constraint 86** (`00G` closed at 85 and split — see CL-0087 footnote); `11A` → when-to-load line, §3 scope-default exception, §4.2 status table (`Locked (admin)`), §4.3 intake channels, §6 conflict check (`Locked (admin)` block branch), §7 (Admin Mode is outside escalation entirely); `11B` → §15 what Update Mode does not do; `11C` → §8 pre-flight carve-out, §9.8 sweep target; `12A` → four jobs, status values, source channels, Part 1 header note (+ the CL-0086/0087/0088 rows); `00C` → FILE REFERENCE INDEX (new **Admin Mode** row); `00D` → **constraint 51 amended** (three parts → four; the lock rule now carries the Admin Mode exception — an Admin change overrides `Locked` and `Locked (admin)` with no unlock); **precedence swept across every other mode topic** — `09A` (Comparison's "takes precedence over every other mode signal" now excepts `ADMIN MODE`), `13_SPLIT_MODE.md`, `15A` (precedence block), `16A` (triage table row + the "`UPDATE MODE` still outranks everything" line), and `00A`'s Mode 7 bullet ("check this FIRST" → "check this before `COMPARISON MODE`", since only `ADMIN MODE` now precedes it); `_project_instructions_.md` → MODE TRIAGE (new first bullet), FILE MAP, OUTPUT EXPECTATION (new Admin Mode entry), Mode 7 precedence line; `INDEX.md` | ADMIN MODE (authorised) — designer request, 28 August 2026 (Chris) | Major | Locked (admin) | Pre-approved by definition — submitted through the authorised channel. **Part 1 (Locked) was EMPTY, so nothing was overridden by this round.** No prior CL row defines a mode trigger or its precedence. |
| CL-0087 | Friday, 28th August, 2026 | **NEW PART 4 — the PageForge Amalgamation Log (`12G`), and NEW constraint 87.** PageForge (the standalone HTML Generator, `pageforge-site` / `CONVERTER_V2`) must eventually produce what this project's instructions produce, so every decision made here about **what the finished module looks like** is one PageForge will have to mirror. Those decisions were already in the Part 3 history — but Part 3 is an *audit trail*, written to answer "what was decided, by whom, when, and what did it displace", and its front-facing rows are interleaved with mechanism rows PageForge has no equivalent of. **THE RULE — the front-facing test, applied by BOTH Admin Mode and Update Mode to every change they action:** *would a developer comparing two generated modules — one built before the change, one after, from the same Writers Template — see any difference in the HTML or CSS?* **YES → Lane 2 (front-facing):** ordinary ledger row **plus** an entry in `12_CHANGE_LEDGER/12G_PAGEFORGE_AMALGAMATION_LOG.md`. **NO → Lane 1 (mechanism):** ledger row **only**. **Borderline → Lane 2** (an unnecessary entry costs one block; a missing one costs PageForge a silent divergence nobody finds until a module is rebuilt). **Front-facing** = which element a writer tag becomes · what content appears or is omitted · hierarchy, nesting, wrappers, grid structure · page scaffold, head, header, footer, menus · page boundaries · class names, ids, data and answer-key attributes, component defaults · any CSS or layout rule · titles and headings · the red-flag / `Writers Note:` / `Note from {author}:` / `Designer/Developer To Do:` messages **as rendered into the page** · acknowledgements format and placement · image output modes and alt text · interactive build rules · any `14` subject global parameter that alters output. **Mechanism** = mode definitions, triggers and triage · **what the Convertor says in the chat** · KB structure, splitting, indexing and committing · the intake, conflict-checking, ledger and approval machinery. **Writing an entry is NOT a work order** — no mode in this project ever edits PageForge's code; the log is a standing, implementable record for a **future** amalgamation pass, at whatever time PageForge development next takes it up. Entry format is one block per decision (`CL-nnnn`, date, scope, source, **writer input that triggers it**, **required output**, supersedes, KB rule, **PageForge status**), appended in CL order newest last; **append-only** — a later decision is a NEW entry naming the earlier in its `Supersedes:` line, and `PageForge status:` is the only field ever edited in place. It splits at 30 KB into `12H_…` like any other part. **BACKFILL: the log starts at CL-0086.** CL-0001 to CL-0085 remain complete and authoritative in the Part 3 history; converting them into this format is a separate one-off pass that has NOT been run, is not automatic, and no mode performs it in passing — flagged to Chris as an available follow-up round. | (c) Universal | **NEW `12_CHANGE_LEDGER/12G_PAGEFORGE_AMALGAMATION_LOG.md`**; **`00H` → NEW constraint 87**; `12A` → "It does four jobs" + Part 4 pointer; `12F` → notes; `11A` → §4.1 (front-facing test added to the every-run ledger duties); `11B` → §10 item 3a, §12 Step 8 pseudo-code, §15, §16 item 3; `11C` → §9.6 sweep target; `17_ADMIN_MODE.md` → §5 (the two lanes, the test, and why the log exists); `00C` → Change Ledger row (three parts → four, incl. Part 4); `00D` → constraint 51 (same four-part amendment as CL-0086); `_project_instructions_.md` → FILE MAP (`12_CHANGE_LEDGER.md` entry); `INDEX.md`. **Housekeeping in the same round (no rule change):** appending constraints 86–88 to `00G` took it to **32,241 bytes**, past the 30 KB soft limit, so per `CLAUDE.md` §4 the three NEW constraints were written into a new part — `00H_CONSTRAINTS_4.md` (8.7 KB, now THE OPEN constraints part) — and `00G` was CLOSED at constraint 85 at its existing 24,199 bytes. **No content was moved:** constraints 75–85 are byte-identical to their pre-round form and `00H` contains only new material (this is a *pre-emptive* split, not the verbatim-move split of CL-0071 / the 13 Aug `11A`→`11C` split). Numbering verified continuous **1–88** across `00D`/`00E`/`00G`/`00H` with no gaps or duplicates; the chain pointers in `00D`, `00E`, `00G` and `INDEX.md` updated to name `00H`; `00_MASTER_INSTRUCTIONS/` part counts 7 → 8 and `12_CHANGE_LEDGER/` 10 → 11; guard clean with **0 warnings** | ADMIN MODE (authorised) — designer request, 28 August 2026 (Chris) | Major | Locked (admin) | Pre-approved — authorised channel. Nothing overridden; no prior CL row covers front-facing/mechanism classification or a PageForge-facing record. |
| CL-0088 | Friday, 28th August, 2026 | **NEW constraint 88 — DESIGNER-FACING OUTPUT POLICY: verify in full, report by exception.** The audience for a conversion run is a **designer, not a developer** — most have no coding background, which is the reason this project exists. A conversion was ending with two dozen lines of div counts, interactive counts, boundary results, template/level confirmations and "all content is visible" ticks, and **a genuinely important message was lost inside them**: on a previous module the writer had repeated the module title as the heading of every page, the conversion followed the writer (correctly) against the project's own convention, and said so — buried among the counts, where the designer did not see it. **THE RULE:** every verification step **still runs in full** (the `02C` checklist, the Phase 7 confirmations, div open/close counts, boundary validation, class-attribute integrity — all of it, unchanged). **Only what reaches the chat changes.** The verbose "post-output verification summary" is replaced by a short **Designer Summary** emitting **only**: **(1) CONVENTION DEPARTURES — always FIRST, always its own labelled block, never merged into a list** — anything produced that is not what the project's rules would normally produce, because the writer's template demanded it, stating **what was done**, **what the rule normally says**, and **where in the writer's template it came from**; **(2) red flags** with pages and what to resolve; **(3) `Designer/Developer To Do:` items**; **(4) `Writers Note:` / `Note from {author}:` messages surfaced** — count and pages only; **(5) anything that could not be done, was guessed at, or fell back**; **(6) any choice still owed by the designer**. **NEVER emitted** (performed silently): div open/close counts, interactive counts and types, data patterns, page-boundary validation, template source and level confirmation, Mode B code/title replacement, heading-pattern / domain / `stickyNav` notes, class-attribute integrity, speech-bubble `no-hover` and click-drop confirmations, acknowledgements placement, "all student content is visible" confirmation, tag normalisation decisions, content-source format and media-list usage. **A FAILED OR UNCERTAIN CHECK IS NEVER SILENT** — it is promoted into (2) as a plain-English red flag. **Silence means checked and clean, never not checked.** Plain English throughout ("the same title appears at the top of every page", not "duplicate `<h1><span>` across page scaffolds"). **This RE-SCOPES every existing "note in the verification summary" instruction across the KB** — such an instruction now means *record it internally and surface it only if it passes the test above*; a correction described elsewhere as silent stays silent. Nothing in (1)–(6) → the summary is **one line**: "No red flags; nothing outstanding." **The generated HTML is byte-identical either way** — a Lane 1 mechanism change under constraint 87, therefore **deliberately NOT entered in the PageForge Amalgamation Log**; Chris named this change as the worked example of the mechanism lane. | (c) Universal | **`00H` → NEW constraint 88**; `02E` → Output Specifications, "Post-output verification summary" REPLACED by "The Designer Summary"; `02C` → new banner under Verification Checklist (run every check, report almost none); `00B` → Phase 7 REPORT block rewritten to the six emit categories + the do-not-report list + the Mode B silence rule; `01A`, `01B`, `06` → re-scoping banner for their "note in the verification summary" instructions; `00B` Phase 6 → the one surviving in-pipeline "NOTE in verification summary" line (Mode B reference-files note) rewritten to record-internally-do-not-narrate; `_project_instructions_.md` → OUTPUT EXPECTATION (Conversion Mode); `INDEX.md` | ADMIN MODE (authorised) — designer request, 28 August 2026 (Chris) | Major | Locked (admin) | Pre-approved — authorised channel. Nothing overridden: no prior CL row governs post-conversion chat reporting. The underlying verification rules (`02C`, Phase 7) are UNCHANGED and no prior row about them is affected. |
| CL-0089 | Friday, 28th August, 2026 | **`learningSupport` on `<html>` is driven by the `X` code prefix.** Learning support modules are identified by `X` as the first letter of the module CODE, and ship `class="notranslate learningSupport"` on every page; the class is appended to the existing list and never alters the derived `template=` value (constraint 21). This is what produces the larger font — delivered wholly by the stylesheet, so the Convertor adds the class and never writes font CSS or an inline font style (constraint 2). **Supersedes the reference-dependent test** in `06` § 4.4 ("the reference files should indicate whether this is needed"): a reference module omitting the class on an X-prefixed code is now corrected in the output, never copied, as with the missing `<body>` tag (constraint 82); a non-X-prefixed module gets the class only if a Mode B sibling carries it, with a `Red Flag:`. Corpus corroboration: `XFUN01_00` (fundamentals), `XDLS9004_03_0` (combo) and `XWHA01-02` (standard 1-3) all carry the class and all are X-prefixed. **Deliberately narrow:** the test governs the CLASS only — § 14.6's LS terminology, tab-nav removal, clickDrop layout, speech-bubble prompts, 6-activity pattern and XLP overviews remain scoped to LS/XLP/XDLS and are NOT extended to XFUN/XWHA/XMES/XTAS. **Extends CL-0020** (LS conventions, unlocked, direct-typed), which already required the class; what changes is how a module qualifies — no ledger decision is overturned. | Cohort — X-prefixed module codes | `06` §2 tree (single occurrence in this repo — the brief anticipated two) + §4.4 (rewritten) + §5 checklist; `14B` §14.6 (bullets 1-2 + scope note); `02C` Template & Structure; `00H` **new constraint 89**; `00C` §14 LS fragment; `12G` | ADMIN MODE (authorised — Persephone Samuels, Design Team Lead, email to Gavin McGruddy 27 August 2026, relayed by Chris) | Major | Locked (admin) | Pre-approved — authorised channel. Nothing overridden at ledger level; the displaced item is the `06` §4.4 prose test, recorded above. |
| CL-0090 | Friday, 28th August, 2026 | **The acks classes GENERATE their statements — stop typing them as well.** `<div class="acks acksTemplate acksAI">` renders three statements from the template: the copyright-holders apology, the AI-tools usage statement, and the Te Kura copyright line. Emitting the same wording as literal `<p>` text double-ups the statement on the published page (`WJFUN105_0.0.html` reported as the example). **Removed from every generated acks block:** the opening apology div, the closing copyright div (and with it the `<span class="currentYear"></span>` token). **Retained:** the `All other images © Te Aho o Te Kura Pounamu, Wellington, New Zealand.` div — it is NOT in the generated set — plus every per-page media entry, unchanged. **The classes are mandatory:** dropping a class in order to type its statement back is never an alternative. **MTK brought into line in the same pass:** `07C`/`07D` shipped a bare `<div class="acks">` with all three statements typed; the wrapper now carries `acksTemplate` and the statements are removed, so MTK follows the universal acks rules in full. **Consequential:** constraint 73 and `02D` permitted-comment exception 6 both said "the three standard boilerplate divs carry no label" — now one div. **CL-0045 / constraint 72 untouched** — `acksAI` is still applied in anticipation of a REQUESTED AI asset with its `Designer/Developer To Do:` note; only the literal text is affected, never the modifier logic. | (c) Universal — no scope was given, so it applies universally per constraint 86 | `05C` Basic block (new generated-statements block) + Accordion structure example (2 divs deleted) + Structure notes (2 bullets rewritten); `07C` §14; `07D` overview skeleton (single acks block; the §19.2 six-tab variant inherits it by reference and needed no separate edit); `02C` (1 item rewritten, 1 added); `02D` exception 6; `00E` constraint 73; `00H` **new constraint 90**; `12G` | ADMIN MODE (authorised — Persephone Samuels, Design Team Lead, email to Gavin McGruddy 27 August 2026, relayed by Chris) | Major | Locked (admin) | Pre-approved — authorised channel. No ledger row overridden; the displaced item is the `05C` "include all three" prose, recorded above. |
| CL-0091 | Friday, 28th August, 2026 | **iStock ID is the FIRST (gm-leading) number — re-confirmed through the authorised channel, and logged for PageForge.** Copyright reported acks in some modules citing the second/trailing number instead of the first, which is the number matching the ID shown on the asset page. **The KB rule was already correct and complete** — CL-0029 / constraint 61 (14 July 2026) settled it: for a dual-ID `gm{A}-{B}` URL the ID is `A`, equal to the asset page's "Stock photo ID" and the preview watermark; the trailing `B` is never used for filenames, acks citations, or acks-file matching. Verified this round at every extraction site (`01E` Mode D + Mode P, `01C` iStock Acknowledgements File, `05C` Entry format, `02C` cross-check) — **no rule text was changed.** The residual exposure is PageForge's own generator and modules built before 14 July 2026, neither of which this project edits. **This row exists to carry the rule into the PageForge Amalgamation Log** (`12G`), which starts at CL-0086 and therefore did not contain CL-0029. This is a targeted, request-driven entry, NOT the general CL-0001–CL-0085 backfill, which remains un-run. | (c) Universal | No rule edits (`01E`, `01C`, `05C`, `02C`, `00E` constraint 61 verified unchanged); `12G` entry added | ADMIN MODE (authorised — Persephone Samuels, Design Team Lead, email to Gavin McGruddy 27 August 2026, relayed by Chris) | Major | Locked (admin) | Pre-approved — authorised channel. Nothing overridden — same result as CL-0029, which stands unchanged and is named in the `12G` `Supersedes:` line as the originating decision. |

---
