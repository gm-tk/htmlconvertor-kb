> **Last updated:** Thursday, 13th August, 2026
> **Granular part E (8 of 9) of `12_CHANGE_LEDGER.md`** — PART 3 footnotes (locked-decision and grouped-change notes).
> All sibling parts live in `12_CHANGE_LEDGER/`; see `INDEX.md` at the repo root. Body below is verbatim source-of-truth content.

<!-- KB-PART-BODY-START -->

> **CL-0001 – CL-0005** are the first genuine changes recorded (direct-typed Update Mode run, 18th June 2026). They were raised under the **pre-19-June model**, in which *every* Major change was actioned provisionally and sent to the design authority for a YES/NO sign-off. **CL-0001 – CL-0004** remain **Pending approval** — mirrored in Part 2, with their approval message already out to the authority. On a **YES** for an item, move its row to Part 1 (Locked) and set its Part 3 status to `Locked`; on a **NO**, revert that change in the affected files and set its Part 3 status to `Reverted`. **CL-0005 was reverted on 30th June 2026 (superseded by CL-0007)** following a designer-confirmed overturn, and no longer appears in Part 2.
>
> **CL-0006** (19th June 2026) changed the model itself. From this point on, the design authority (Persephone) is engaged **only** for report-vs-report conflicts, and non-conflicting changes are actioned directly and collated into a finalized difference report for the designer (Gavin). The still-pending **CL-0001–CL-0004** are deliberately **left as-is** under the model in force when they were raised — they are *not* retroactively re-routed, and their existing approval messages stand. **CL-0007** (30th June 2026), which supersedes CL-0005, was actioned under the current model: a designer-confirmed overturn of a prior *unlocked, direct-typed* decision is direct-typed and non-conflicting (no report-vs-report clash), so it was implemented directly without Persephone escalation.
>
> **CL-0008 and CL-0009** (30th June 2026) action the PageForge V1.5 requirements report (whitelisted reviewer-comment rendering, and SPLIT MODE / Page Stitcher packaging). Both are Universal-scope Major changes. The report is a **direct-typed (Update Mode) requirements document, not a finalized Comparison Mode difference report**, so it can never trigger report-vs-report escalation; both changes were conflict-checked against Part 1 (empty) and Part 3 (no clash), found non-conflicting, and **implemented directly without Persephone escalation**. They are independent of each other.
>
> **CL-0010, CL-0011, and CL-0012** (1st July 2026) action a further direct-typed Update Mode requirements document that refines the three areas above: source-specific red-note prefixes rendered red **and bold** (CL-0010), Split Mode **one-file-per-response** emission (CL-0011), and `PAGEFORGE-GUIDE` **manual-stitch comment blocks** (CL-0012). All three are Universal-scope Major changes that arrived **direct-typed (not a finalized Comparison Mode difference report)**, so none can trigger report-vs-report escalation. Conflict-checked against Part 1 (empty → nothing is blockable) and Part 3: **CL-0010 supersedes the reviewer-comment prefix established in CL-0008** — the canonical rendering becomes `<p style="color: red; font-weight: bold;">Note from {author}: …</p>`, and CL-0008's historical description is preserved verbatim for audit with only its Status annotated; **CL-0011 refines** the Split Mode output cadence and **CL-0012 extends** the Split Mode comment policy, both first set in **CL-0009** (likewise preserved, Status annotated). Because every prior decision involved is **unlocked** and direct-typed, the three changes were implemented directly without Persephone escalation. **None weakens a guardrail:** every designer-facing note remains visible (now bold), and the `PAGEFORGE-GUIDE` blocks carry no student content, designer-action notes, or interactive answers and are machine-stripped by the Page Stitcher (mirroring CL-0009's splice markers).
>
> **CL-0013 – CL-0023** (2nd July 2026) action a large **direct-typed Update Mode requirements batch** — the design team's per-subject **global-parameter** documents (Languages Phase 1–4, Pathways, Taonga, CED Phase 5, FUNdamentals H&PE, LS, BLL, HPE content lessons) plus a Teams transcript and notes — folded in so the Convertor applies them automatically. **CL-0014** creates a **new file `14_SUBJECT_GLOBAL_PARAMETERS.md`** (Fork A) as the scope-tagged home for these conventions; **CL-0013** adds a **fourth red-note prefix `Designer/Developer To Do:`** (Fork B1, extending the unlocked CL-0010) so every deferred "TO DO / TBC / placeholder / under development" item is baked in as a **visible** red-bold placeholder (Fork C); **CL-0015–CL-0022** record each subject family's conventions at its confirmed scope; **CL-0021** carries a designer-confirmed **BLL carve-out from constraint 43** (`.dropbox` not appended to `.activity`); **CL-0023** records the **"Upload to Dropbox"** series label (BLL/LS/HPE) alongside the universal "Go to dropbox" default. The whole batch arrived **direct-typed (not a finalized Comparison Mode difference report)**, so none can trigger report-vs-report escalation; conflict-checked against **Part 1 (empty → nothing blockable)** and **Part 3 (no prior decision on these subject rules)**, all found non-conflicting and **implemented directly without Persephone escalation**. **No guardrail weakened:** the new prefix is visible + bold (the sole permitted inline style, constraint 2), and the CED "commented-out printable links" suggestion was deliberately rendered as a **visible** Designer/Developer To Do note rather than an HTML comment (no new permitted-comment exception granted — a commented-out-links approach would need a separate sign-off).
>
> **CL-0024 – CL-0028** (9th July 2026) action a **mixed-intake** Update Mode run: two **direct-typed** subject global-parameter documents (BLLR, MiW/WJ) alongside two **finalized Comparison Mode difference reports** (BLL263, BLL266). **Part 1 (Locked) is empty, so nothing was blockable.** **No report-vs-report conflict arose**, so no catalog was raised for Persephone. Two report items resolved to **"already in effect"** rather than edits — BLL266 D2's entry-format fields (CL-0027, already delivered by CL-0003) and BLL266 D4's requested exclusion (already subsumed by `09` §4.1 Exclusion 1; broadened for clarity as CL-0028). Only BLL266 D2's **title-sourcing sub-rule** was genuinely new (CL-0026), and it **reverses a prior `01` statement** that video titles come from the Media List — that statement arrived by project instruction, not a finalized report, so it is a direct supersession rather than an escalation. **BLL263 was deferred in full at the designer's instruction:** Difference 1 (template level `1-3` → `4-6`) is **NOT actioned this run** — the level test remains unsettled (the report's menu-form rationale contradicts the `06` §3.1 / `01` label tables, which map `I can:` to Years 7–8/9–10, and the proposed code-suffix test did not isolate Years 4–6) — and Difference 2 (BLL overview tab-split) was not reached. **Both remain open for a future Update Mode run.** **No guardrail weakened:** the two new `14` families introduce no CSS/JS and no invented classes (every class/code block was supplied by the design team — constraint 2), all deferred assets surface as **visible** `Designer/Developer To Do:` notes (constraint 59), and CL-0028 suppresses *reporting* only, never conversion behaviour.
>
> **CL-0029 – CL-0034** (14th July 2026) action a **mixed-intake** Update Mode run applied in a single pass at the designer's instruction: one **direct-typed fix specification** (the iStock dual-ID extraction defect report, CHFUN01 trigger case → CL-0029), two **finalized Comparison Mode difference reports** (BLL253 D4 → CL-0030; BLL262 D1 → CL-0031), and two **direct-typed designer emails** (Languages global template changes → CL-0032 / CL-0033; ARTs global template changes → CL-0034). **Part 1 (Locked) was empty at run time — nothing was blockable — and no report-vs-report conflict arose, so no catalog was raised for Persephone.** CL-0031 **settles the BLL263 Difference 1 template-level question deferred on 9 July** (same `1-3`→`4-6` result, now designer-confirmed via the module-code→level mapping rather than the rejected menu-form test) and resolves the BLL266 `template="1-3"` inconsistency going forward; **BLL263 Difference 2 (BLL overview tab-split) remains open.** CL-0029 supersedes an informal, never-documented trailing-segment filename convention (recorded here so the reversal is auditable) and leaves the alt-text / acks-file-authority rules (CL-0001 / CL-0002, constraints 52–53) untouched. CL-0032–CL-0034 additively extend the unlocked, direct-typed CL-0015 / CL-0017 family sections — no prior decision was overturned, so no designer-confirmation pause was required beyond the emails themselves. **No guardrail weakened:** the two new constraints (61–62) tighten behaviour; every deferred asset (tui characters, unconfirmed Languages central folders) surfaces as a **visible** `Designer/Developer To Do:` note (constraint 59); `imageCentral` is applied only to design-team central-store assets, preserving the `01` reservation against writer-image use; and every one-interactive-per-activity split is flagged with a visible `Red Flag:` note.
>
> **CL-0035** (14th July 2026, same-session addendum) records a designer-directed **overturn of CL-0031**, issued and explicitly confirmed minutes after it (per `11` §6 — a direct-typed change against an unlocked, report-sourced prior pauses for the designer's own confirmation and is never catalogued for Persephone): the **BLL2xx sub-series (any code with the literal prefix `BLL2`) is phase 1 → `template="1-3"`**, the series default `"4-6"` stands for any other BLL code, and **BLLR2xx stays phase 2 → `"4-6"`** (a `BLLR…` code never matches the `BLL2` prefix — fourth character `R`). CL-0031 is marked `Reverted — superseded by CL-0035`; its derive-from-code / never-mirror principle survives, restated inside CL-0035. Net effect on the questions CL-0031 had answered: **BLL263 Difference 1 re-settles as `"1-3"`**, and the **BLL266 `template="1-3"` "inconsistency" dissolves** — the shipped value was correct for a 2xx code. Production follow-up (outside Update Mode): the designer-refined **BLL262 pages currently carry `"4-6"` and need `"1-3"`**; any other built BLL2xx pages should be spot-checked the same way.
>
> **CL-0036 – CL-0041** (16th July 2026) action a **direct-typed** Update Mode batch of four uploaded documents: an "additional notes and dev requests" note (CL-0036 widened-activity text width; CL-0037 Creative-Services Vimeo videos), a designer differences log compiled from Eliza Rogers' review with binding CLAUDE TO ACTION instructions (CL-0038 `[MTKquiz]` → "Go to quiz"; CL-0039 acks published-title-verbatim incl. ". stock photo"), and two self-contained implementation specifications written for this actioning session (CL-0040 canonical overview module-menu; CL-0041 supervisor `super-content-button` family). **Part 1 (Locked) was empty — nothing blockable — and every intake channel was direct-typed (none is a finalized Comparison Mode difference report), so no report-vs-report conflict is possible and no catalog was raised for Persephone.** The designer's in-message instruction *"these changes are to override any previously actioned change"* stands as the in-session confirmation `11` §6 requires for any supersession of a prior unlocked decision; in the event, no ledgered decision was overturned — CL-0036 extends CL-0007, CL-0039 extends CL-0001/CL-0002 (alt-text rule reaffirmed), CL-0038 fills a gap (no prior `[MTKquiz]` rule existed), and CL-0040/CL-0041 supersede **never-ledgered pre-ledger documentation** (the "TWO tabs"/mimic-the-reference overview rules and the `supervisorContainer` trio), recorded here so the reversals are auditable. **Normalisation & interpretation decisions surfaced for the designer:** the Vimeo note prefix folded into the standard `Designer/Developer To Do:` red+bold scheme (supplied lead was "Designer/Developer to action", red non-bold); the Vimeo scaffold's `src` quoted, its `title="YouTube video player"` kept verbatim pending designer confirmation, and its `<!-- &amp;start=0 --> <!-- &amp;end=0 -->` placeholders granted as narrow permitted comment exception 5; MTKquiz writer content kept visible on-page (no silent deletion) and the `dropbox` wrapper token treated as a leftover; the canonical overview shell scoped to the **tabbed** archetype so simplified-overview series (`10` §2/§4) and the open BLL263 D2 question are untouched; `09` Exclusion 2 retained with a dated status note. **No guardrail weakened:** every new note is visible red+bold; exception 5 carries no content/note/answer; CL-0040/CL-0041 tighten structure. Housekeeping: the stale "files `00`–`12`" range in `00`'s timestamp convention and constraint 50 corrected to `00`–`14`.
>
> **CL-0042 – CL-0052** (16th July 2026) action the **OSSC401 finalized Comparison Mode difference report** (10 included differences, 17 scoped (d) and omitted by the report itself) **plus one direct-typed designer instruction issued in-session** (CL-0052). **Part 1 (Locked) is empty, so nothing was blockable, and no report-vs-report conflict arose — no catalog was raised for Persephone.** The report is the run's only finalized-report intake; CL-0052 is direct-typed and therefore can never trigger report-vs-report escalation. **Three items were resolved with the designer at pre-flight before any file was touched:** (1) **CL-0042's scope contradiction** — the report assigned (b) OSSC-wide on evidence from a single `template="9-10"` module while editing the Years 9–10/NCEA dual-title rule; the designer confirmed **(b) as written**, so the rule now binds OSSC at levels where the dual-title rule never applied; (2) **CL-0046's wording** — the report flagged that the designer's own file still read `<!-- Page 4.0 -->` on its last acks group; the designer confirmed **`Lesson N.N`** (the `Page 4.0` is a stray); (3) **CL-0051's guardrail** — the emoji rule is the **first-ever exception to constraint 1**, permitting the Convertor to *delete* writer characters; the designer explicitly confirmed the guardrail change, the `Red Flag:` prefix (which the report left open), the standard note wording, and the exempt tick/cross set, per `11` §8.3. **Two report items resolved as partially "already in effect" rather than new rules:** **CL-0048** (activity text/interactive two-row split) requests the **same result** as the unlocked, direct-typed **CL-0036** of the same day but without its "widened wrapper" precondition — so constraint 63 was **amended in place** rather than duplicated by a new constraint that would have contradicted it; and the `02` verification-checklist item cited by **CL-0043** needed no edit, since it bans `<p>` *between a heading and its list*, which a lead-in above the first heading does not do. **A documentation gap was closed in the same pass** (as the report advised): the acks `<!-- Lesson N.N -->` annotation was used throughout the `05` worked example but appeared in **no** permitted-comment list — now recorded as exception 6 in `02` and in `00` constraint 37. **CL-0052 overturns the report-to-Gavin limb of CL-0006** — a direct-typed change against an unlocked, direct-typed prior, confirmed explicitly in-session per `11` §6.3 and applied to this very run (no difference report was produced). **No guardrail weakened on balance:** CL-0051's exception is disclosed in a **visible** red note rather than silent, leaves wording untouched, and exempts meaning-bearing glyphs; CL-0046's new comment exception carries no content, note, or answer; CL-0047 and CL-0049 suppress *reporting* only, never conversion behaviour; and CL-0052 removes an internal reporting artefact, not any visible designer-facing note in the HTML.

---

