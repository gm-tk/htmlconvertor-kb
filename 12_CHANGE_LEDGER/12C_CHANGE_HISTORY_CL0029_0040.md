> **Last updated:** Thursday, 13th August, 2026
> **Granular part C (3 of 9) of `12_CHANGE_LEDGER.md`** — PART 3 change history: CL-0029 to CL-0040 (continued).
> All sibling parts live in `12_CHANGE_LEDGER/`; see `INDEX.md` at the repo root. Body below is verbatim source-of-truth content.
> *The table header row below is repeated for readability; the rows continue the PART 3 history table.*

<!-- KB-PART-BODY-START -->
| ID | Date | Change summary | Scope | Affected file(s) / rule(s) | Source (intake channel) | Class | Status | Authority decision |
|----|------|----------------|-------|----------------------------|--------|-------|--------|--------------------|
| CL-0029 | Tuesday, 14th July, 2026 | **iStock dual-ID URLs: the iStock ID is the `gm`-leading number.** For `gm{A}-{B}` URLs (the current iStock format, e.g. `gm2194652495-612793002`) the ID is **`A`** — the number immediately following `gm`, equal to the asset page's "Stock photo ID" and the preview watermark ID; the trailing `B` is **never** used for filenames (`images/iStock-{A}.jpg`), acknowledgements citations, or iStock-acks-file matching (a trailing-segment lookup falsely reports "no matching line"). Legacy single-ID URLs (`gm957693546`) unchanged; both formats now documented side-by-side at every extraction site, plus a validation-suite **iStock ID cross-check** (filename ID = gm-leading URL capture = acks citation). **Supersedes any informal trailing-segment working convention** — never documented in any project file (full-KB grep confirmed only the two legacy examples); observed as defective behaviour in CHFUN01 Mode D output (`iStock-612793002.jpg` emitted where `iStock-2194652495.jpg` is correct). Alt-text (CL-0001 / constraint 52) and acks-file-authority (CL-0002 / constraint 53) rules untouched — this governs which NUMBER is the ID, not which text is the title. | (c) Universal | `01` → Mode D filename rule 1 + Mode P commented-out iStock tag rules + iStock Acknowledgements File (How To Use It); `05` → Acknowledgements (Entry format — ID consistency); `02` → verification checklist (iStock ID cross-check); `00` → **new constraint 61** + Pipeline Phase 7 | Direct-typed (Update Mode) — "iStock ID Extraction Defect — Investigation & Fix Specification", 14 July 2026 (CHFUN01 trigger case) | Major | Implemented | Designer decision (Gavin / Chris); not escalated — a direct-typed fix specification, not a finalized Comparison Mode report; Part 1 empty, and no prior ledger decision on ID extraction exists (BLL262 D2's iStock-filename difference was scoped (d) ignore-once and never actioned). Classified Major per `11` §5 — (c) Universal is always Major (the source report proposed Routine) |
| CL-0030 | Tuesday, 14th July, 2026 | **One interactive per activity (universal).** Each `activity` wrapper contains **at most ONE interactive component**; when the writer places two or more interactives under a single activity heading, split them into separate sequential activities (the first keeps the writer's activity number, each subsequent interactive takes the next letter) and renumber the following activities (e.g. a trailing 1E Word Find becomes 1F), flagging the split with a visible `Red Flag:` note. Supporting elements (audio buttons/triggers, images, hover translations, instructional text) are not counted. Closes the documentation gap BLL253 D4 identified — COMP_00 required the activity wrapper but stated no per-activity limit. | (c) Universal | `03` → COMP_00 Universal Rules (new critical rule); `00` → **new constraint 62** + Pipeline Phase 5 (enforcement step); `02` → verification checklist (Interactive Components); `05` → Activities (cross-reference) | Finalized difference report (BLL253, Difference 4) | Major | Implemented | Non-conflicting — Part 1 empty; no prior decision on interactive count anywhere in Part 3, so no report-vs-report clash; actioned directly and collated into the designer's difference report |
| CL-0031 | Tuesday, 14th July, 2026 | **BLL template level corrected: Years 4–6 (`template="4-6"`).** BLL modules the pipeline previously assigned Years 1–3 (`template="1-3"`) are **Years 4–6** — the template value is derived from the module-code→level mapping and **never mirrored from Mode B siblings** (BLL261 / BLL253 / BLL266 ship the superseded `"1-3"`, the exact fault BLL262 D1 records). **Settles the previously deferred BLL263 Difference 1** (same `1-3`→`4-6` result; deferred on 9 July 2026 because its menu-form level *test* was unsettled — BLL262 supplies the designer-confirmed result via the code→level mapping instead) and resolves the open BLL266 `template="1-3"` inconsistency in the same direction going forward. BLL263 Difference 2 (overview tab-split) remains open. | (a) Series + level — BLL series, the modules previously assigned Years 1–3 | `06` → §5 Validation Checklist (Detection — Template level); `14` → §14.7 (new template-level bullet, cross-referencing `06` §5); `00` → FILE REFERENCE INDEX (§14 BLL fragment) | Finalized difference report (BLL262, Difference 1) | Major | Reverted — superseded by CL-0035 | Non-conflicting — Part 1 empty; BLL263 D1 was never actioned (no prior decision existed) and requested the SAME result, so there is no report-vs-report clash to catalog; borderline class treated as Major per `11` §5 (a skeleton-attribute change across a series) **Superseded 14 July 2026:** the designer's direct-typed BLL2xx phase-1 exception (CL-0035) reverses the `"4-6"` assignment for the 2xx sub-series, confirmed explicitly in-session per `11` §6; the derive-from-code / never-mirror principle and the non-2xx series default are restated within CL-0035 |
| CL-0032 | Tuesday, 14th July, 2026 | **Languages: central `imageCentral` character assets.** The recurring Languages characters/personas are centralised template assets served from the design team's per-language central folders (`German assets/`, `Chinese assets/`, `Japanese assets/`, `Samoan character assets/` observed) with `class="img-fluid imageCentral" loading="lazy"`, per the updated `20260511_Language_HTML` reference ("updated to the central images"). An unconfirmed central folder/filename → visible `Designer/Developer To Do:`. Writer-specified module images unchanged (`images/…`, never `imageCentral` — the `01` reservation is intact). | Cohort — the 6 Languages prefixes, Phases 1–4 | `14` §14.1 (new Central-images bullet); `00` → FILE REFERENCE INDEX (§14 Languages fragment) | Direct-typed (Update Mode) — designer email "Languages global template changes for HTML converter" + attached `20260511_Language_HTML.html` | Major | Implemented | Designer decision (Gavin / Chris); **extends the unlocked, direct-typed CL-0015** (additive — no prior image-sourcing decision to overturn); not report-vs-report, so not escalated |
| CL-0033 | Tuesday, 14th July, 2026 | **Languages are ALL combo.** Every page of every Languages-cohort module ships `template="combo"` on `<html>` (the sub-type — Fundamentals / Inquiry / standalone — still comes from the `<body>` class, `06` §2), and `06` §5's Mode B template-level check now expects `combo` for this cohort rather than a year band. Consequence recorded: combo is not one of the three autoCheck auto-apply template files (constraint 38), so autoCheck applies in Languages only when writer behaviour calls for it. | Cohort — the 6 Languages prefixes, Phases 1–4 | `14` §14.1 (new All-combo bullet); `06` → §5 (Template level — Languages note); `00` → FILE REFERENCE INDEX (§14 Languages fragment) | Direct-typed (Update Mode) — designer email "Languages global template changes for HTML converter" ("Languages are all combo (should be reflected in the code)") | Major | Implemented | Designer decision (Gavin / Chris); **extends the unlocked, direct-typed CL-0015** (no prior template-value decision to overturn); not report-vs-report, so not escalated |
| CL-0034 | Tuesday, 14th July, 2026 | **Taonga (The Arts): Tui character markup supplied — art still DEFERRED.** The ARTs global-template update supplies the central-asset convention for the phase-varying tui character: folder `tui_characters/`, files `tui_phase_1.jpg`–`tui_phase_4.jpg` keyed to the module's phase, supplied markup `<img class="img-fluid imageCentral" loading="lazy" src="tui_characters/tui_phase_1.jpg" alt="Tui character" >`. The characters remain **under development** → the scaffold is emitted with a visible `Designer/Developer To Do:` note; an unstated module phase → `Red Flag:`. **Refines CL-0017's deferred Tui Toi item** (placeholder-only → documented markup + phase filenames, still deferred). | Cohort — Taonga modules (all phases) | `14` §14.3 (Tui bullet expanded); `00` → FILE REFERENCE INDEX (§14 Taonga fragment) | Direct-typed (Update Mode) — designer email "ARTs global template changes for HTML converter" | Major | Implemented — **superseded by CL-0073** (10 August 2026) | Designer decision (Gavin / Chris); refines the unlocked, direct-typed CL-0017; not report-vs-report, so not escalated. Intake labelled "ARTs" — applied to `14` §14.3 **Taonga (The Arts)**, the section already documenting the phase-varying tui character; flagged in the run report for the designer to redirect if "ARTs" meant a different family. **Superseded 10 August 2026:** CL-0073 replaces the flat one-image-per-phase path (`tui_characters/tui_phase_N.jpg`) with per-phase folders and multiple poses (`tui_characters/phase_{N}/tui_{P}.jpg`), and **lifts the art deferral** — the illustrations are now delivered, so the "still under development" `Designer/Developer To Do:` note is retired. What survives from this row: the phase-derived folder selection, the `Red Flag:` on an unstated phase, and the supplied `class`/`loading`/`alt` attributes, all restated within CL-0073. Description above preserved verbatim for audit |
| CL-0035 | Tuesday, 14th July, 2026 | **BLL2xx sub-series exception: phase 1 → `template="1-3"`.** Any BLL module whose code begins with the literal prefix `BLL2` (BLL253, BLL261, BLL262, BLL263, BLL266, …) is **phase 1** and ships `template="1-3"` on every page — NOT the series-default `"4-6"`. **Overturns CL-0031 (same day, designer-confirmed in-session per `11` §6):** the concrete `"4-6"` assignment is reversed for the 2xx sub-series — which covers every currently known BLL module, including BLL262 itself, whose designer-refined pages (the CL-0031 evidence) now need `"1-3"` in production — while the series default (`"4-6"` for any other BLL code) and the derive-from-code / never-mirror principle (constraint 21) are restated within this rule. Re-settles BLL263 Difference 1 as `"1-3"`; dissolves the BLL266 "inconsistency" (its shipped `"1-3"` was correct). **Contrast pinned: BLLR2xx modules are phase 2 → `template="4-6"`** — a `BLLR…` code never matches the `BLL2` prefix (fourth character `R`), so the exception never applies to BLLR. | (a) Series + level — BLL series, the 2xx sub-series (BLLR contrast pinned) | `06` → §5 (Template level — BLL split rewritten); `14` → §14.7 (rewritten) + §14.9 (new BLLR template-level bullet); `00` → FILE REFERENCE INDEX (BLL / BLLR fragments) | Direct-typed (Update Mode) — designer instruction in-session, 14 July 2026 | Major | Implemented | Designer decision (Chris) — overturn of the unlocked, report-sourced CL-0031 confirmed explicitly in-session per `11` §6 (a direct-typed-vs-report clash is never escalated to Persephone); CL-0031 marked `Reverted — superseded by CL-0035`. Designated a permanent standing rule; a Part-1 lock (immunity to future finalized reports) remains available via Persephone sign-off on request |
| CL-0036 | Thursday, 16th July, 2026 | **Inside a widened activity wrapper, plain text stays at `col-md-8`.** When an activity wrapper is widened per constraint 56 (`col-12` Standard / `col-md-11 col-12` Inquiry & Fundamentals / `col-md-12 col-12` full width), the widened width serves the interactive only — the activity's prose (headings, instructions, paragraphs, lists) sits at `col-md-8 col-12` in its own inner row, the interactive in its own full-width inner row. Requested "only if not currently enforced" — a full KB check confirmed no existing rule stated an inner-text width for widened wrappers, so this is a genuinely new rule, not a duplicate. | (c) Universal | `00` → **new constraint 63** + Pipeline Phase 6; `02` → Interactive Wrapper Width (new rule + pattern) + internal constraint 31 + verification checklist; `05` → Activities + Columns & Floating Columns | Direct-typed (Update Mode) — "additional-notes-and-dev-requests" document | Major | Implemented | Designer decision (Gavin / Chris); not escalated — direct-typed, no report-vs-report conflict; additively extends the unlocked CL-0007 wrapper-width scheme |
| CL-0037 | Thursday, 16th July, 2026 | **All Creative Services videos are Vimeo links.** A CS-produced (Te Kura in-house / Audiovisual) video embeds as the designer-supplied Vimeo `videoSection` scaffold with the ID left pending (`src="https://player.vimeo.com/video/"`), preceded by a visible red+bold `Designer/Developer To Do: add vimeo embed for [audiovisual item x]` note. Scaffold emitted exactly as supplied, including the trailing `<!-- &amp;start=0 --> <!-- &amp;end=0 -->` time-parameter placeholders — recorded as **permitted comment exception 5** (machine/scaffold mechanism; no content, note, or answer). **Normalisations recorded:** the supplied note lead "Designer/Developer to action" (red, non-bold) was folded into the standing four-prefix scheme as `Designer/Developer To Do:` red+bold (CL-0010/CL-0013; wording after the prefix preserved); the supplied unquoted `src=` attribute was quoted for HTML validity; the supplied `title="YouTube video player"` attribute was kept verbatim and flagged for the designer to confirm. External YouTube videos unchanged. | (c) Universal | `05` → Video Embed (**new Creative Services Videos (Vimeo) subsection**); `01` → Media → Video; `02` → Comment & Red Flag Policy (**new permitted exception 5**) + internal constraint 52 + verification checklist; `07` → Video References (cross-ref); `14` → §14.1 (Videos bullet cross-ref); `00` → **new constraint 64** + philosophy comment-exception list + Pipeline Phase 6 | Direct-typed (Update Mode) — "additional-notes-and-dev-requests" document | Major | Implemented | Designer decision (Gavin / Chris); not escalated — direct-typed, no report-vs-report conflict; the comment-exception grant comes from the designer-supplied code itself and carries no answer/note (no guardrail weakening) |
| CL-0038 | Thursday, 16th July, 2026 | **`[MTKquiz]` emits a "Go to quiz" button — never a dropbox button.** Any `[MTKquiz]` activity (any modifiers — `[engage]`, `[type the answer]`, `[Teacher marked …]`) ends in `<a href="#" target="_blank"><div class="button">Go to quiz</div></a>` (blank href — the developer wires in the D2L quiz quicklink) plus a visible `Designer/Developer To Do:` note to create the new quiz within MTK (My Te Kura). **Interpretations recorded:** writer-supplied quiz content attached to the activity stays rendered on the page (constraint 1 / no silent deletion — the developer transfers it into the quiz; the refined example's deleted `<ul>` is a production step, not a conversion rule); the `dropbox` wrapper modifier does not attach (the refined example's leftover `activity dropbox` token is recorded as a leftover); the red note uses the standard `Designer/Developer To Do:` prefix. `[MTKquiz]` added to the `01` tag taxonomy (`mtk_quiz`). Constraint 55's "Go to dropbox" label rule (CL-0004) untouched — different button. | (c) Universal | `01` → Tag Taxonomy (Link/Button Tags) + Section 05 Links & Buttons; `05` → Buttons (**new MTK Quiz Button subsection**) + Activities; `14` → §14.8 (Quiz rules cross-ref); `02` → verification checklist; `00` → **new constraint 65** + Pipeline Phase 6 | Direct-typed (Update Mode) — designer differences log (Eliza Rogers review), Difference 1 CLAUDE TO ACTION | Major | Implemented | Designer decision (Chris — "override any previously actioned change" confirmed in-message); not escalated — the log is a direct-typed designer instruction set, not a finalized Comparison Mode report, and no prior ledger decision mapped `[MTKquiz]` (the dropbox output came from the generic deferred-submission pattern, never a documented MTKquiz rule) |
| CL-0039 | Thursday, 16th July, 2026 | **Acks entry titles are the official published title verbatim — including a trailing ". stock photo".** A stock-asset acknowledgement title is cited exactly as published (sentence casing, internal punctuation, and any trailing ". stock photo" suffix when part of the official iStock title — trigger case iStock 1137701281, *"Tourist visiting lake Wanaka, New Zealand. stock photo"*). Slug-derived titles are an approximation and yield to the official title (iStock acks file / asset page) wherever available; never title-case or "correct" the published title. **The alt-text prohibition on "stock photo" (CL-0001 / constraint 52) is explicitly unchanged and still enforced** — alt text is a brief screen-reader description and drops platform-title suffixes. Extends the unlocked CL-0002 verbatim-authority principle to the derived-title route. | (c) Universal | `05` → Acknowledgements (Entry format — **new published-title-verbatim bullet**; Sourcing — slug bullet rewritten); `01` → Media List Companion Document + Images (alt text contrast note); `02` → verification checklist; `00` → **new constraint 66** | Direct-typed (Update Mode) — designer differences log (Eliza Rogers review), Difference 2 CLAUDE TO ACTION | Major | Implemented | Designer decision (Chris); not escalated — direct-typed; additively extends the unlocked CL-0001/CL-0002 (no overturn: alt-text rule reaffirmed, acks-file authority untouched) |
| CL-0040 | Thursday, 16th July, 2026 | **Canonical overview (`-00`) module-menu tab set — reference mirroring retired.** The tabbed `-00` menu is built from the fixed canonical set **Overview → Knowledge → Practices → Information → Standards/Assessment** (shell transcribed from `refresh_NCEA_overview 3.html`; `tooltip="Overview"` on `#module-menu-button`), with the content-driven **omission rule** (absent content → remove BOTH `<li>` and `.tab-pane`; never synthesise), **match-by-meaning / label-by-canon** writer-naming tolerance, **one-variation-per-panel** rules (Overview Var 1/2; Standards Var A ≤3 / Var B >3), the canonical **heading & column table** (`<h4><span>` Overview/Knowledge/Practices titles; `<h5>` no-span labels + all Information/Standards headings — supersedes the former all-`<h4>`/never-`<h5>` overview rule and the "TWO tabs" description), the canonical success title **"How will I know I have learned it?"**, the **scroll rule** (`overflowYScroll scroll="500"`), and stripped `Designer note:` lines. NCEA (501) folded into the same canonical set (its three-tab form = the omission rule at work). **Reconciliations recorded:** the rule governs the **tabbed** archetype — series with legitimately simplified/absent overview menus (`10` §2/§4, e.g. BLL / ENG strands) keep that archetype, so the open BLL263 D2 (BLL overview tab-split) question is NOT touched; `09` §4.1 Exclusion 2 (overview-menu heading reporting suppression) retained as-is with a dated status note; tooltip placement moves to the button per the canonical shell. Lesson-page simplified menus unchanged. | (c) Universal | `01` → Module Menu Structures (**`-00` section rewritten in full**, "When to Use Which" + NCEA section rewritten) + Mode B derivation steps 3/6/7 + worked example; `06` → §4.3 + §5 checklist; `10` → §2 (tab-composition note); `09` → §4.1 Exclusion 2 (status note only); `02` → verification checklist (overview-menu items rewritten); `00` → **new constraint 67** + Pipeline Phase 1/7 | Direct-typed (Update Mode) — "Overview Module-Menu — Implementation Specification" (self-contained spec, actioned this run as instructed) | Major | Implemented | Designer decision (Gavin / Chris); not escalated — direct-typed spec, no report-vs-report conflict; supersedes never-ledgered `01` documentation (the "TWO tabs" / mimic-the-reference / all-`<h4>` rules predate the ledger); Part 1 empty — nothing blockable |
