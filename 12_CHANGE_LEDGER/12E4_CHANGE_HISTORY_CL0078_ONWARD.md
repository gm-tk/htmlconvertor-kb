> **Last updated:** Friday, 21st August, 2026
> **Granular part E4 (7 of 9) of `12_CHANGE_LEDGER.md`** — PART 3 change history: CL-0078 onward (continued) — THE OPEN PART: APPEND NEW ENTRIES HERE.
> All sibling parts live in `12_CHANGE_LEDGER/`; see `INDEX.md` at the repo root. Body below is verbatim source-of-truth content.
> *The table header row below is repeated for readability; the rows continue the PART 3 history table. Opened 13 August 2026 when `12E3` approached the 30 KB soft limit and was closed at CL-0077 (`CLAUDE.md` §4).*

<!-- KB-PART-BODY-START -->
| ID | Date | Change summary | Scope | Affected file(s) / rule(s) | Source (intake channel) | Class | Status | Authority decision |
|----|------|----------------|-------|----------------------------|--------|-------|--------|--------------------|
| CL-0078 | Thursday, 13th August, 2026 | **SCES302 Difference 11 — an unlabelled standalone external-link button reads "Go to website".** Constraint 75 fixes the *element* a standalone `[external link]` becomes but left the button's **text** unspecified, so the conversion took whatever words the writer attached to the link — including, at SCES302 Activity 6A, the destination's own name ("Gravity and Orbits"), which reads as a title rather than a call to action. The rule now records the default and the boundary the designer's files draw: writer text that already reads as a **call to action** is kept **verbatim** ("Go to Stellarium website", kept unchanged at that module's Activity 1A); writer text that is merely the **resource's title**, **or no label at all**, takes the generic **"Go to website"**. The resource's own name is never used as button text. Documented as a three-row test table in `05` → COMP_14 → Buttons, immediately above the existing submission-label paragraph, and cross-referenced from constraint 75 | (c) Universal | `05B` → COMP_14 → Buttons (**new default-label rule + test table + worked markup**, placed alongside the constraint-55 labels), `00G` (constraint 75 — default-label sentence added), `INDEX.md` | Finalized difference report — SCES302, Difference 11, scope (c), 10 August 2026 | Routine | Implemented | Designer scope decision (c) Universal. Conflict check clean — **Part 1 (Locked) is empty**. Explicitly **additive** to the neighbouring label conventions, exactly as the report's actioning note requires: constraint 55's "Go to dropbox" / "Go to portfolio" (`CL-0004`, `CL-0023`) and constraint 65's "Go to quiz" (`CL-0038`) govern **different buttons** and are untouched, as is the BLL/LS/HPE "Upload to Dropbox" series label (§14.11). `CL-0050`, which created constraint 75, is unlocked and is **extended, not reversed** — it settled the element, this settles the label |
| CL-0079 | Thursday, 13th August, 2026 | **SCES302 Difference 13 — NEW Comparison Mode Exclusion 9: the filename a media asset carries in `images/` is never reported.** Covers departures from **every** documented Mode D filename-construction pattern (iStock, Wikimedia / Creative Commons, Unsplash, writer-supplied and fallback descriptors alike) and any difference of casing, separator, extension or descriptor wording within them — e.g. `images/wiki-milky-way.jpg` → `images/1076_Milky_Way.jpg`, or `wiki-halleys-comet-animation.gif` → `Halley's_Comet_animation.gif`, where a Wikimedia asset keeps its source filename instead of the documented `wiki-{short-descriptor}` kebab-case pattern. Rationale: the developer settles final asset filenames during production, so a filename difference does not indicate a conversion fault. **Reporting suppression only — the Mode D filename-construction rules are unchanged and still applied when generating.** **The exclusion is deliberately scoped to the filename STRING**, with an explicit warning block listing what it must NOT be read to suppress: an image-**mode** fault (a `placehold.co` URL in a Mode D build, or the reverse), a missing or extra `<img>`, an `alt`-text difference, a missing `class`/`loading` attribute, and above all **the iStock ID of constraint 61** — the ID must still match across the filename, the acknowledgements citation and the acks-file lookup, and a wrong ID remains a reportable fault even though the filename wrapped around it is not | (e) Ignore always | `09B` → §4.1 (**new Exclusion 9** + its scoping warning), `09B` → Applying the exclusions (range **1–8 → 1–9**), `INDEX.md` | Finalized difference report — SCES302, Difference 13, scope (e), 10 August 2026 | Routine | Implemented | Designer scope decision (e) Ignore always, actioned per the report's verbatim instruction block for Update Mode. Conflict check clean — **Part 1 (Locked) is empty**. **Overlap noted and deliberately allowed to stand:** Exclusion 7 (`CL-0047`, WJFUN108) already suppresses a narrower case of the same thing — a Mode D *derived* filename swapped for the actual supplied asset filename. Exclusion 9 is the general form and subsumes it; Exclusion 7 is left in place rather than merged, because both carry their own provenance and removing a numbered exclusion would renumber the range every later entry cites. The `Applying the exclusions` range was bumped per the standing `11C` §9 blast-radius rule (in `11A` §9 until the 13 August split) (precedent `CL-0047`, `CL-0049`) |
| CL-0080 | Thursday, 13th August, 2026 | **NEW §14.12 Technology — five delivered strand icons, and the celebration gif is DELIVERED (lifting the HPE deferral).** Technology had no subject-global-parameters section at all. **(1) STRAND ICONS.** Five central-store SVGs in **`Technology/strand/`**, carrying `class="img-fluid imageCentral"` like every other central asset, recorded as an exact-filename table: `active-Spacial-and-Design.svg`, `active-Materials-processing.svg`, `active-Electronics.svg`, `active-Digital-Technology.svg`, `active-Computer-Science.svg`. **The filenames are copied character-for-character and never "corrected"** — `Spacial` is the design team's own spelling and the capitalisation is inconsistent between files (`Materials-processing` vs `Digital-Technology`); these are the real server paths, and a tidied path is a broken path. The `alt` carries the readable strand name and is not bound to the filename's spelling. **(2) THE CELEBRATION GIF IS DELIVERED.** `congradulations/Congradulations animation.gif` now exists as a real asset. It is the **same central asset** the HPE family has carried as a placeholder since `CL-0013`-era deferral, so **§14.8's "still under development" deferral and its standing `Designer/Developer To Do:` note are RETIRED** — the same treatment `CL-0073` gave the Taonga tui when its art landed. **(3) THE MISSPELLING IS A RECORDED DESIGN DECISION AND STAYS.** *Congradulations* is misspelt in the folder, the filename and the `alt`. Modules carrying it are **already built and live**, and the string is **never visible to a learner** (a path, plus an `alt` on a decorative graphic), so it is left exactly as it is — never silently corrected, never flagged as a typo, never emitted in a "fixed" variant. Both §14.8 and §14.12 now carry this in terms, so a future session cannot tidy it. **(4) LAYOUT.** The suggested Technology celebration layout is `col-8` text beside `col-4` gif, recorded as the design team's **suggestion**, not a hard rule | Cohort — Technology modules (plus the §14.8 HPE deferral lift) | `14B` (**NEW §14.12**, appended after §14.11; §14.8 celebration bullet — deferral retired + misspelling note; §14.11 central-assets bullet; part banner) — *which pushed `14B` past the 30 KB soft limit, so §14.11 and the new §14.12 were split verbatim into a new `14D_SGP_CROSSCUTTING_AND_TECHNOLOGY.md` per `CLAUDE.md` §4, with the `14A`/`14C` part banners bumped to "of 4"*, `00E` (constraint 60 — documented-cohort list gains Technology), `00C` (§14 file-reference row), `INDEX.md` | Direct-typed designer instruction (Chris, 13 August 2026) — `technology-module-changes.txt` | Major | Implemented | Designer instruction (Chris, 13 August 2026): *"here are some assets for technology that have been completed. They are some icons and a celebration gif"*, and on the spelling, explicitly: *"I am aware … that congradulations is misspelled, however, there are now modules with this incorrect spelling already developed and made Live, and since the word isn't actually visible to the users the decision has been made to leave it."* Conflict check clean — **Part 1 (Locked) is empty**; the HPE deferral being lifted (`CL-0013` pattern, restated in §14.8) is unlocked and direct-typed, so a direct-typed delivery supersedes it without escalation, per the `CL-0073` precedent. **Two gaps recorded rather than invented:** the **module-code prefix** for Technology is not stated in the source, so the family is scoped by subject with an explicit "do not guess a prefix" note until one is supplied; and the new `col-8`/`col-4` celebration layout **sits oddly beside §14.8's** description of the same gif as "1×1 and too large to sit in a `col-8`, redesign the layout around it" — both are recorded as their own family's guidance and the tension is flagged for a designer decision rather than silently resolved |
| CL-0081 | Thursday, 13th August, 2026 | **PageForge Compare Mode: the `{CODE}_interactives.txt` worklist STOPS being a required input — the tester uploads PageForge's ORIGINAL HTML and nothing else. Reverses limb (2) of `CL-0072` and rewrites constraint 81(b).** Chris's instruction: *"I want the developer to only upload the HTML files that were initially generated from PageForge WITHOUT needing to upload the `{CODE}_interactives.txt` file, since the raw information for each interactive will be contained in the writers template which will have already been uploaded at the start of the chat thread."* **WHY IT HOLDS — the evidence was never only in the worklist; it is on PageForge's own page.** `CL-0072` justified the requirement with three jobs, and on an **un-stitched** page the page itself answers all three: **(a) who built it** — a `cv2-int-ref` reference box IS PageForge stating it did not build that interactive, and finished widget markup on an original PageForge page is therefore PageForge's own build **by definition, there being no third possibility before stitching**; **(b) the boundary** — the box's collapsed `cv2-int-raw` block is precisely the writer content PageForge captured (what the worklist's `Content:` line carried) and the activity wrapper the box sits inside is PageForge's ownership decision (what its `Activity:` line carried), with the writer's template supplying the other side of the comparison; **(c) inventory reconciliation** — a cross-check only, whose primary source was always the page. **THE REQUIREMENT THAT REPLACES IT: the pages must be PageForge's ORIGINAL, un-stitched output.** All of that evidence lives in the `cv2-*` markup and PageForge's **Page Stitcher removes it**, so a module already taken through Mode 6 and stitched cannot say whether a finished widget is PageForge's or Claude's, nor what boundary PageForge chose. Chris's own wording — *"the HTML files that were **initially generated** from PageForge"* — settles this. **AND PROVENANCE IS ASKED, NEVER INFERRED** — one line at the start of the run (*"are these PageForge's original files, or have they been through the Page Stitcher?"*). Counting `cv2-*` markers cannot answer it and is explicitly forbidden as the test, because **the inference fails in both directions**: a PageForge run that built EVERY interactive also carries no markers, so "no markers ⇒ stitched" would suppress every class C and build-quality finding on PageForge's **best** possible result; and a **partly** stitched module still shows markers (the stitcher leaves an unmatched one in place and warns rather than failing), so "markers ⇒ original" would attribute Claude's Mode 6 builds to PageForge and report them to Gavin as PageForge's output — the precise error the mode exists to prevent. The tester's answer establishes provenance; the markers only corroborate. The §5.3 state table is reordered with an explicit **first-match-wins** precedence (volunteered worklist entry → confirmed-original → unconfirmed), keeping the per-interactive rule of constraint 80 intact. Only where the answer is "stitched", "unsure" or "mixed", and the originals no longer exist, does the mode run with the pre-existing documented limits (finished markup marked `state unknown`, no class C finding, boundary judged from the page alone). What was the exception path under `CL-0072` (missing worklist) is now the exception path for a different and rarer cause (stitched upload), and the `state unknown` enum is relabelled `state unknown — provenance unconfirmed`. **Two supporting gaps closed in the same pass:** the reference box's **visible label** is now shown in `16A` §5.1's example markup and documented as the carrier of the `{type}` segment (it is what class C reads instead of the worklist's `Type:` line — corroborated by `15B`, and previously elided as "…label row + arrow…"), and `16C`'s class-C worked example, which still cited a `Worklist entry:` line as the type's source over a snippet showing a label-less box, now reads the type off the label. **A worklist a tester volunteers anyway is still accepted as CORROBORATING EVIDENCE, never a Mode 6 work order** — `CL-0072`'s evidence-not-a-work-order rule survives intact, so an attached `.txt` can never silently start Mode 6; its **absence is normal and is never remarked on in the report**. `CL-0072`'s other two limbs — **(1) never advertise the mode** and **(3) comments/developer notes excluded outright** — are untouched and still binding | (c) Universal — Mode 7 | `16A` (part banner; §1 trigger — three inputs, never ask for the worklist; §1.2 precedence — a volunteered worklist is corroboration; §2 tester-workflow steps 1 and 5; **§4 FOUR REQUIRED INPUTS → THREE**, X row removed; **§4.3 rewritten end to end** — "Upload PageForge's ORIGINAL output — and why no worklist is needed", the page-carries-the-evidence argument, the stitched-upload warning + limits, the volunteered-worklist rule; §5.1 — signature reworded, the visible label added to the example markup and documented as the type carrier; §5.2 — the stitched signature no longer instructs the reader to infer provenance from absent markers; §5.3 state table rewritten and reordered with explicit first-match-wins precedence; §6 workflow — signature drops `X_worklist`, step 0 never asks for the worklist and **asks the tester for provenance instead of inferring it**, steps 1 and 5 read the box), `16B` (part banner; §3.2 step 3 — read the owning activity off the page, stitched-page caveat; §4 — type read from the box's visible label; class-C evidence bundle; §5.3), `16C` (report header inputs line + upload-format enum; the state-unknown note re-caused to stitching/unconfirmed provenance; the class-C worked example's type source; finding-bundle evidence line; reference-code source; inventory enum `state unknown — worklist not supplied` → `state unknown — provenance unconfirmed`; §7 close), `00A` (Mode 7 section + triage bullet), `00C` (file reference row), `00F` (when-to-load row — trigger condition + the conditional `15B` load), `00G` (**constraint 80** input phrase; **constraint 81 opening sentence + limb (b) rewritten**), `15A` (§1 Precedence — Mode 7 does not ask for a worklist), `_project_instructions_.md` (Mode 7 triage bullet, file map), `INDEX.md` | Direct-typed designer instruction (Chris, 13 August 2026) | Major | Implemented | Designer decision (Chris, 13 August 2026), actioned as an **explicit override**. Conflict check: **Part 1 (Locked) is empty**, so nothing is blockable. The change **does** contradict a live universal constraint — 81(b) — which under `11C` §8 item 6 (added this same day by `CL-0076`) must be named rather than quietly followed: the constraint is named here, and Chris's instruction is treated as the confirmed override it is, rewriting the constraint rather than making a one-off exception. `CL-0072` is `Implemented`, **unlocked** and **direct-typed**, so a direct-typed supersession is actioned without escalation (`11A` §6 — report-vs-report is the only escalation trigger, and neither side here is a report); `CL-0071`, which made the worklist optional in the first place, is effectively restored and extended. Classified **Major** per `11A` §5 (universal scope, changes a documented input contract). **One point recorded rather than assumed:** Chris's stated reason is that the writer's template carries the raw information, which is true of the *writer's* side of every comparison; the *PageForge* side comes from the reference boxes, which is why the un-stitched-original requirement is written in as the replacement condition rather than left implicit — without it the removal would silently cost class C findings and every stitched interactive's boundary |
| CL-0082 | Friday, 21st August, 2026 | **`[MTKquiz]` builds a four-element activity shell and the Convertor NEVER generates the quiz's own content — REVERSES the "writer-supplied quiz content stays visible" limb of `CL-0038`.** Constraint 65 previously specified only the tail of the activity (a blank-href "Go to quiz" button + a red note) and explicitly *kept* the writer's questions rendered on the page so the developer had the data to build the quiz from. The designer's instruction settles the **whole box** and removes that content. **(1) THE ACTIVITY BOX.** An `[MTKquiz]` always gets its own `activity` wrapper carrying the **next consecutive activity number, even where the writer assigned none** — a bare `[MTKquiz]` dropped in after Activity 1C becomes **Activity 1D**, and the page renumbers on from there; the Convertor never emits an unnumbered quiz block and never re-uses the preceding number. **(2) EXACTLY FOUR CHILDREN, IN ORDER:** the **quiz title** as the FIRST child (`<h3>Quiz</h3>` by default, or the writer's own quiz title verbatim where supplied); any **quiz instructions the writer wrote for students** as normal paragraph text beneath it; the visible red+bold **`Designer/Developer To Do:`** note instructing the designer to create the quiz in **MTK DEV** and **orgunit link** it to this module; and the **`<a href="#" target="_blank"><div class="button">Go to quiz</div></a>`** button. Nothing else appears in the box. **(3) NEVER THE QUIZ CONTENT.** Questions, options, correct answers, model answers and any sentence/passage material the quiz will use are **not emitted in any form** — not visible, not listed, not in the red note, not in an HTML comment. **Designer decision on the disclosure question: the omission is SILENT** — no `Red Flag:`, no trace — because the designer builds the quiz from the **Writers Template**, which still holds the original content. Recorded in **constraint 1** as its second documented removal and its **only sanctioned undisclosed** one (the emoji removal of constraint 74 remains disclosed). **Normalisations surfaced for the designer:** the supplied markup wrote the blank href as `href=""`; on the designer's instruction the documented `href="#"` form is **retained** unchanged. `09` Exclusion 8 (CL-0064) is **retained, not deleted** — it is now largely spent (there is normally no rendered question set left for a designer to remove) and carries a dated status note scoping it to pre-CL-0082 modules and proofs, with an explicit warning never to report the *absence* of quiz content as a difference. | (c) Universal | `05D` → **MTK Quiz section rewritten** (activity shell, four-element order, worked markup, the never-generate rule); `00E` (constraint 65 rewritten); `00D` (**constraint 1** — second documented removal, silent); `00B` → Pipeline Phase 6; `01E` → `mtk_quiz` output mapping; `01D` → WJ precedence note; `02C` → verification checklist (3 items replace 1); `05B` → COMP_14 Activities; `03C` → COMP_02 MCQ WJ note; `14B` → §14.8 quiz rules; `09B` → Exclusion 8 status note; `INDEX.md` | Direct-typed (Update Mode) — designer dev request 01, 21 August 2026 | Major | Implemented | Designer decision (Chris). **Part 1 (Locked) is EMPTY — nothing blockable.** This **supersedes an unlocked, direct-typed prior decision** (`CL-0038`, 16 July 2026), so per `12A` / `11` §6–7 it is **not** escalated to the design authority — a clash with a non-report channel pauses for the designer's confirmation, and the designer is the instructing party here. The reversal is limited to CL-0038's *content-visibility* limb: its "Go to quiz" button, blank href, red note, no-dropbox-button and no-`dropbox`-modifier limbs all **stand unchanged**, as do `CL-0058` (WJ precedence) and `CL-0064` (Exclusion 8, rescoped by status note, not revoked). The constraint-1 carve-out was raised explicitly with the designer before actioning and the silent form was chosen deliberately |
| CL-0083 | Friday, 21st August, 2026 | **NEW constraint 83 — `loading="lazy"` is NEVER added to an image inside a moving-or-draggable interactive; WIDENS the former sketcher-only prohibition.** Lazy loading defers an image until the browser judges it near the viewport, which is right for ordinary page images and wrong for any image that moves: it can arrive late, mid-animation, or with the wrong measured size, breaking positioning, animation and hit-detection. The project had this rule for **one** component only (the sketcher `.canvasContainer`, comment-policy constraint 28) while `03B` positively **instructed** `loading="lazy"` on every drag-and-drop image. **THE NAMED LIST (designer-chosen scope — "moving or draggable", not "every interactive"):** rotating banner `.rotateBanner`/`.bannerContainer`/`.bannerItem`; carousel `.carousel`/`.viewer`/`.item`; drag and drop `.dragAndDrop` (`.drag`, `.drop`, `.ddContainer`, `.ddColumn`, incl. `ddColumn` header images); click drop `.clickDrop`/`.clickDropContent`; flip card `.flipCard` (`.front`, `.front flipImage`, `.back`); memory game `.memoryGame` (`.memCard`, `.cardHidden`); and the sketcher `.canvasContainer`. Applies in **both image output modes**, to real images and `placehold.co` placeholders alike. **EXPLICITLY UNAFFECTED — these KEEP `loading="lazy"`:** images the student only clicks or hovers without any movement (`infoImage`/`infoTrigger` hotspots, `imageLabel` diagrams), plus `alertImage`, speech-bubble characters, accordion and tab-panel images, and self-reflection emoji. **The forward test recorded for components not yet listed:** does an image inside it move, animate, or get dragged by the student? Yes → drop the attribute; no → keep it. Every worked example in the affected component sections was corrected to the new form (4 in `03B`, 2 flip-card in `04A`, 1 memory-game in `03E`, 1 D&D in `07B`), and `03B`'s "Use `loading="lazy"` on all images" bullet and `07B`'s "below the fold" bullet were rewritten. | (c) Universal | `00G` → **new constraint 83** (the open constraints part); `01E` → Images → Rules Common to Both Modes (**new prohibition section + component table + the forward test**) and the Mode D attribute bullet; `02D` → comment-policy constraint 28 **renamed** `never_lazy_load_sketcher_images` → `never_lazy_load_moving_interactive_images` and widened; `02C` → verification checklist (2 new items + sketcher item cross-ref); `03B` → D&D key rules + 4 examples; `03E` → memory game image-card rule + example; `04A` → carousel, rotating banner, click drop and flip-card sections (+2 examples); `04C` → COMP_11 sketcher note (sibling-components pointer); `07B` → MTK image common rules + D&D example; `INDEX.md` | Direct-typed (Update Mode) — designer dev request 02, 21 August 2026 | Major | Implemented | Designer decision (Chris). **Part 1 (Locked) is EMPTY — nothing blockable.** No prior ledger row decided lazy-loading scope: the sketcher prohibition is **pre-ledger** documentation, and this **extends** it rather than reversing it (the sketcher stays prohibited, on the same reasoning). The scope boundary was put to the designer as three options — moving/draggable only, plus click-target images, or every image inside any interactive — and **"moving or draggable"** was chosen, which is why `infoImage`/`imageLabel` are named as explicitly retained rather than left ambiguous. Constraint 2 (no invented CSS/JS) and the image-output-mode rules are untouched — this changes one attribute's presence, nothing structural |

---
 
