> **Last updated:** Thursday, 13th August, 2026
> **Granular part D (4 of 9) of `12_CHANGE_LEDGER.md`** — PART 3 change history: CL-0041 to CL-0062 (continued). **CLOSED** at CL-0062 — new entries continued in `12E2_CHANGE_HISTORY_CL0063_0071.md`, now closed; the OPEN part is `12E4_CHANGE_HISTORY_CL0078_ONWARD.md`.
> All sibling parts live in `12_CHANGE_LEDGER/`; see `INDEX.md` at the repo root. Body below is verbatim source-of-truth content.
> *The table header row below is repeated for readability; the rows continue the PART 3 history table.*

<!-- KB-PART-BODY-START -->
| ID | Date | Change summary | Scope | Affected file(s) / rule(s) | Source (intake channel) | Class | Status | Authority decision |
|----|------|----------------|-------|----------------------------|--------|-------|--------|--------------------|
| CL-0041 | Thursday, 16th July, 2026 | **Supervisor buttons: `super-content-button` family; `supervisorContainer` trio retired.** Triggers: `[supervisor note]` / `[supervisor button]` / `[supervisor]` + the red-text `Supervisor:` / `Supervisor's Notes:` prose lead-in (now a supervisor trigger, carved out of `Writers Note:` handling). Shapes: **A** activity-integrated (`super-content-button` + `number` on `.activity`; reveal panel FIRST child), **B** section standalone (class on the `.col`), **C** section paired (`paddingL` button col + `paddingR` content col); **headline rule** — outermost `<div class="row supervisor">` in all shapes (sole exception: nested inside another widget, which keeps the widget's row); invariant 4-level reveal-panel nesting (JS-critical); default `col-md-8 col-12`; default `<h3>Supervisor note</h3>` (writer heading wins); existing activity modifiers kept, `super-content-button` appended; `[side alert]` affects placement only. The legacy `supervisorContainer`/`supervisorButton`/`supervisorContent` trio (2 files corpus-wide, designer-replaced on sight) and the minority `activity supervisor` form are **recognise-only — never emit**. | (c) Universal | `05` → **Supervisor Button section rewritten in full**; `01` → Tag Taxonomy (supervisor variants) + Section 05 Links & Buttons (`supervisor_button` output row rewritten) + Red Text Handling (supervisor prose-lead carve-out); `02` → verification checklist + **new internal constraint 55**; `00` → **new constraint 68** + Pipeline Phase 6/7 | Direct-typed (Update Mode) — "Supervisor Button — Implementation Specification" (corpus-evidenced spec, actioned this run) | Major | Implemented | Designer decision (Gavin / Chris); not escalated — direct-typed spec, no report-vs-report conflict; supersedes the never-ledgered `supervisorContainer` documentation in `01`/`05` (predates the ledger); Part 1 empty — nothing blockable |
| CL-0042 | Thursday, 16th July, 2026 | **OSSC series: the lesson-page `<h1>` carries the LESSON title, not the module title.** For OSSC (Online Safety — Scams) modules at **every** year level, a lesson page (`-01`, `-02`, …) carries a **single** `<h1><span>` holding that page's own lesson title (from the lesson's opening H2, minus any `Lesson N` / `Lesson N:` prefix), and that title is **not** repeated as a body `<h3>`. Overrides BOTH cited rules for OSSC: the universal "lesson `<h1>` = MODULE title" rule **and** the "YEARS 9–10 and NCEA lesson pages (dual title)" rule — an OSSC lesson page carries one `<h1><span>`, never the English + Te Reo pair, at any level. The `#module-code` lesson-number `<h1>` and the OSSC **overview** page (module title, dual where its level requires) are unchanged. The related "drop a body heading that duplicates the `<h1>`" rule survives and simply bites differently for OSSC. **Scope caveat recorded:** the evidence is OSSC401 alone (`template="9-10"`); the designer chose (b) OSSC-wide over the evidence-matching (a) OSSC-at-9–10 when the contradiction was surfaced pre-flight, so at OSSC levels 1-3/4-6 this rule now applies where the dual-title rule never did. | (b) Module-series — OSSC (all levels) | `01` → Template Title Patterns → Lesson Page Header (both rules); `00` → **new constraint 69** | OSSC401 finalized difference report, Difference 1 | Major | **Implemented — GENERALISED to universal by `CL-0069` (3 August 2026); constraint 69 superseded by constraint 79** | Non-conflicting — Part 1 (Locked) empty; no prior ledger decision on lesson-page title composition. Scope confirmed by the designer in-session after the (b)-vs-(a) contradiction was raised at pre-flight. **CL-0069 note:** the scope caveat recorded here proved understated in the other direction — corpus measurement (1,105 lesson-title pages vs 252 module-title) showed this was never an OSSC exception but the library-wide norm, so the rule was widened to every series rather than overturned. OSSC output is byte-unchanged |
| CL-0043 | Thursday, 16th July, 2026 | **OSSC series: `[Lesson Overview]` lead-in paragraph in the lesson module menu.** For OSSC modules at every year level, where the writer's `[Lesson Overview]` supplies a descriptive sentence ("Ākonga will…") ahead of the learning/success labels, it renders as a `<p>` **above the first `<h5>`**, inside the same `col-md-8 col-12`. **Not a reversal of the existing prohibition** — which bans `<p>` elements *between a heading and its list*; the lead-in sits above the first heading, so both rules hold. The paragraph is **writer content** (constraint 1, verbatim), not label text, and is **not** subject to constraint 24 label normalisation. Absent sentence → no lead-in, not an error. | (b) Module-series — OSSC (all levels) | `01` → Module Menu Structures → Lesson Pages — Simplified Module Menu (rule + example + structural-rules bullet); `00` → **new constraint 70** | OSSC401 finalized difference report, Difference 2 | Routine | Implemented | Non-conflicting — Part 1 empty; no prior decision on lesson-menu lead-in text. The `02` verification-checklist item the report cited needed no edit: it bans `<p>` *between headings and lists*, which this rule does not touch |
| CL-0044 | Thursday, 16th July, 2026 | **Footer navigation `href`s are left EMPTY (universal).** Every footer nav anchor ships `href=""` — `prev-lesson`, `next-lesson`, `home-nav` alike; the conversion never computes sibling filenames (`MODULE_CODE-XX.html`) into them, as the links are wired at publish time in D2L. The per-position `<li>` composition rule (overview / middle / final) and the `id`/`class`/`target` attributes are unchanged. **Mode B reconciled:** the former "copy the footer structure but replace ALL navigation hrefs to use the NEW module code" instruction assumed populated hrefs and is superseded — a reference's populated hrefs are **emptied**, never re-pointed. | (c) Universal | `01` → Footer and Acknowledgements (Navigation hrefs rule rewritten + Mode B note); `00` → **new constraint 71** | OSSC401 finalized difference report, Difference 3 | Major | Implemented | Non-conflicting — Part 1 empty; no prior ledger decision on footer hrefs |
| CL-0045 | Thursday, 16th July, 2026 | **A REQUESTED AI asset triggers `acksAI`, even though it does not exist at conversion time.** The `acksAI` test reads **forward** to what the finished module will contain, not to what exists at conversion: a writer request for an AI-generated asset (e.g. red-text *"CS: could you create an image for this please?"*) applies `acksAI` **in anticipation**, paired with a visible `Designer/Developer To Do:` note that the asset and its acks entry are still to be created. A **non-AI** asset request does not trigger it. **Unclear production method → apply `acksAI` and say so** (trivially removable; a missing `acksAI` on a shipped AI asset is a compliance miss). Closes the timing gap the report identified — a conversion can never see an asset a later CS request will produce. | (c) Universal | `05` → Acknowledgements → Basic block (wrapper); `00` → **new constraint 72** | OSSC401 finalized difference report, Difference 4 | Major | Implemented | Non-conflicting — Part 1 empty; extends the existing `acksAI` rule rather than overturning it (the modifier and its markup are unchanged — only the timing condition is resolved) |
| CL-0046 | Thursday, 16th July, 2026 | **Acks page-label annotation reads `Lesson N.N`, never `Page N.N` — and is a permitted comment exception.** Each **media-carrying** `acksLesson` div opens with `<!-- Lesson 0.0 -->`, `<!-- Lesson 1.0 -->`, … matching the `acksLesson` class name and the team's usage. **Gap closed in the same pass** (as the report advised): the annotation was used throughout the `05` acks worked example but was **absent from every permitted-comment list** — `00` constraint 37, `02` constraint 52, and the `02` Comment & Red Flag Policy — which named only the Mode P image reference, the MTK `<!-- CS: Item N -->` key, the Vimeo placeholders, and the Split Mode markers. Now recorded as **permitted comment exception 6**: a structural grouping label carrying no content, note, ambiguity, or answer. The three boilerplate divs (apology / "All other images ©…" / Te Kura copyright) carry no label. | (c) Universal | `05` → Acknowledgements → Accordion structure (example ×3 + structure note); `02` → Comment & Red Flag Policy (**new permitted exception 6**); `00` → constraint 37 (exception list) + **new constraint 73** | OSSC401 finalized difference report, Difference 6 | Major | Implemented | Non-conflicting — Part 1 empty. **Designer confirmed the `Lesson` wording in-session**, resolving the report's own flag that the designer's file still read `<!-- Page 4.0 -->` on the 4.0 group (a stray, not the intent) |
| CL-0047 | Thursday, 16th July, 2026 | **`09` §4.1 — new Exclusion 4: designer supplying media metadata the conversion could not confirm.** Comparison Mode no longer reports the designer filling in acknowledgement media metadata that the conversion correctly placeholdered or red-flagged (e.g. resolving `[title to be confirmed]` / `[author to be confirmed]` to the real published title and channel). Supplying metadata the source did not carry is expected designer production work, not a rule fault — the conversion behaved exactly as `05` → Video entries requires. **Reporting-suppression only — no conversion rule changes:** unconfirmable media metadata is still placeholdered/red-flagged, never invented. | (e) Ignore always | `09` → §4.1 (**new Exclusion 4**) + Applying the exclusions (range 1–3 → 1–5) | OSSC401 finalized difference report, Difference 7 | Routine | Implemented | Non-conflicting; (e)-scope changes only the Comparison Mode exclusions list, never conversion behaviour (`11` §11). Distinct from the existing Exclusion 1 (red-note prefixes/styling) and Exclusion 3 (bespoke presentation), so a genuinely new exclusion rather than a broadening |
| CL-0048 | Thursday, 16th July, 2026 | **Activity prose and interactive sit in SEPARATE inner rows — in every activity, not only widened ones.** Inside an `activity` wrapper, the heading/intro prose sits at `col-md-8 col-12` in its own inner `row`, and the interactive follows in a **separate** inner `row` at the wrapper's own width — **whether or not the wrapper is widened**. **Extends the unlocked CL-0036** (16 July, direct-typed), which established exactly this text/interactive split but conditioned it on the wrapper being **widened** per constraint 56; the OSSC401 evidence shows the same split on default-width activities, so the "widened-only" precondition is removed and the rule generalised. Per `11` §6.2 the widened case was **already in effect** and was not re-actioned — only the generalisation is new. Text-only activities need no split. **Report's `col-md-8`-for-prose result is unchanged from CL-0036** — this is a scope widening, not a reversal. | (c) Universal | `05` → Activities (interactive-activity example + width rule rewritten) + Columns & Floating Columns; `02` → Interactive Wrapper Width + verification checklist + internal constraint 31; `00` → constraint 63 (rewritten — "widened" precondition removed) | OSSC401 finalized difference report, Difference 17 | Major | Implemented | Non-conflicting — Part 1 empty; **same result** as the unlocked, direct-typed CL-0036, so no overturn and no report-vs-report clash (`11` §6.2/§6.3). No new constraint number: constraint 63 is amended in place, since a second constraint stating a broader form of the same rule would contradict it |
| CL-0049 | Thursday, 16th July, 2026 | **`09` §4.1 — new Exclusion 5: per-instance presentational modifier classes with no documented trigger.** Comparison Mode no longer reports the designer adding/removing a **documented but trigger-less** presentational modifier on an otherwise correctly-built component, applied per-instance to taste (trigger case: `noBG` added to the 1A `dragAndDrop` but not the 4A one in the same module). These are styling calls that vary activity-to-activity within a single module, not rule faults. **Scope limit recorded:** modifiers that DO carry a documented trigger (e.g. `autoCheck`'s template-based auto-application, constraint 38) are **not** covered — a wrong `autoCheck` stays reportable. **Reporting-suppression only.** | (e) Ignore always | `09` → §4.1 (**new Exclusion 5**) + Applying the exclusions (range) | OSSC401 finalized difference report, Difference 20 | Routine | Implemented | Non-conflicting; (e)-scope, no conversion behaviour changed. Deliberately **not** folded into Exclusion 3 (bespoke presentation/composition), which covers bespoke *compositions and substitutions*, not modifier toggling on a correctly-chosen component |
| CL-0050 | Thursday, 16th July, 2026 | **A standalone `[external link]` builds an `externalButton`; an inline one stays an anchor — position decides, not the tag.** A link the writer gave its own line/paragraph (a call to action) renders as `<a …><div class="externalButton">…</div></a>`; a link inside prose, a list item, or a table cell renders as a plain inline `<a>`. `[external link button]` always renders as a button regardless of position. **The `external link` / `external link button` tag distinction is RETAINED** in the taxonomy — only the *rendering* of a standalone `[external link]` changes — exactly as the report's actioning note required (the same OSSC401 page keeps `[external link]` inline in the "Who can help?" table and the page-02 Snopes alert). Test recorded: would removing the link leave a grammatically incomplete sentence? Yes → anchor. Containment in an alert/accordion/speech bubble does not change the test. Borderline → render the anchor + `Red Flag:`. | (c) Universal | `01` → Tag Taxonomy → Link/Button Tags + Section 05 Links & Buttons output mapping (split standalone/inline); `05` → Buttons (**new positional rule + examples**); `00` → **new constraint 75** | OSSC401 finalized difference report, Difference 23 | Major | Implemented | Non-conflicting — Part 1 empty; no prior ledger decision on external-link rendering. Constraint 55 ("Go to dropbox" labels, CL-0004) and constraint 65 (MTK quiz button, CL-0038) are untouched — different buttons |
| CL-0051 | Thursday, 16th July, 2026 | **No emoji in modules — strip, list-ify, and disclose the removal. FIRST-EVER EXCEPTION TO CONSTRAINT 1.** Finished modules carry no emoji: the Convertor **removes** them from writer content — the sole documented case permitting *deletion* of writer characters rather than re-casing. **Exempt (kept verbatim): ticks and crosses** `✅ ✓ ✔ ☑ ✗ ✘ ❌ ❎` — they carry meaning (correct/incorrect, done/not done) rather than decoration; the exempt set was confirmed by the designer in-session, extending the report's "ticks and crosses are okay" to the full glyph family. Rules: leading emoji removed with its separating whitespace; **a run of 2+ consecutive emoji-prefixed lines becomes a `<ul>`/`<li>` list** (a lone one stays a `<p>`); emoji inside prose removed and spacing normalised to a single space (`be kind ❤️ to yourself` → `be kind to yourself`); wording otherwise untouched (typos and all — e.g. "isa trap" preserved). **Disclosure:** one visible note per affected page at the first removal — `<p style="color: red; font-weight: bold;">Red Flag: Emoji have been removed from this content per the no-emoji rule. Ticks and crosses are retained.</p>`. **Prefix `Red Flag:` confirmed by the designer** (the report left it open): a Convertor-made change to writer content is neither the writer's note nor a pending production task. Borderline glyph → **keep + red-flag**, never strip on a guess. Self-reflection emoji **images** (`self-reflection-emoji/*.png`) are unaffected — images, not characters. | (c) Universal | `01` → Special Characters (emoji line rewritten) + **new "No Emoji in Modules" section**; `02` → Source-Specific Red-Note Prefixes (**new Convertor-made-change rule + standard wording**); `00` → **constraint 1 amended (documented removal exception)** + **new constraint 74** | OSSC401 finalized difference report, Difference 26 | Major | Implemented | Non-conflicting — Part 1 empty; no prior decision on emoji handling. **Guardrail-level change: explicitly confirmed by the designer in-session** per `11` §8.3 before any file was edited — the designer intends constraint 1 to permit emoji deletion. Guardrail preserved in substance: the removal is **disclosed** in a visible red note (never silent), wording is untouched, and meaning-bearing glyphs are exempt |
| CL-0052 | Thursday, 16th July, 2026 | **Update Mode NEVER produces a finalized difference report for the designer (Gavin).** No Update Mode run generates, writes out, presents, or offers a difference report of its actioned changes — regardless of intake channel, classification, or scope. The run's own output **is** the record: the restated change list, the conflict report, the regenerated files, the per-file change log, and this ledger. Do not offer one; do not close a run by reminding anyone to email one. **Overturns the report-to-Gavin limb of CL-0006** (19 June 2026, direct-typed, unlocked), which established the collate-and-email-to-Gavin behaviour; **the rest of CL-0006 stands** — Persephone is still engaged only for report-vs-report conflicts, and the **Persephone conflict catalog is unaffected** (a different artefact on a different path, `11` §7.1). Applied to the run in which it was issued: no report was produced for this run. | (c) Universal | `11` → PURPOSE + "in short" + §7 + §12 pseudo-code Step 9 + §15 + §16 (items 4 and 7 + closing line); `00` → Mode 4 definition + FILE REFERENCE INDEX + constraint 49 + **new constraint 76**; `_project_instructions_` → OUTPUT EXPECTATION (Update Mode) | Direct-typed (Update Mode) — designer instruction in-session, 16 July 2026 | Major | Implemented | Designer decision (Chris) — **overturn of the unlocked, direct-typed CL-0006 confirmed explicitly in-session** per `11` §6.3 ("whenever carrying out the UPDATE MODE from now on, DO NOT generate a difference report for Gavin… enforce this into the project"). A direct-typed-vs-direct-typed clash is **never** catalogued for Persephone. CL-0006's Part-3 row annotated `partially superseded by CL-0052` (report-to-Gavin limb only); its authority-routing limb remains in force |
| CL-0053 | Monday, 27th July, 2026 | **Knowledge base migrated to the `htmlconvertor-kb` GitHub repository (granular part files); Update Mode's deliverable changed from full-file regeneration to the Repo Update Brief.** The sixteen knowledge files were split byte-identically into per-topic folders of lettered parts, all under 30 KB (migration proof: the repo's `tools/kb_manifest.json`), guarded by `tools/check_kb.py` (40 KB hard size limit, provenance headers, index completeness, CL-id sequence integrity), a pre-commit hook, and CI. Update Mode no longer regenerates or outputs any project file: each run produces ONE **Repo Update Brief** — numbered precise edits (exact old wording → exact new wording) with granular part paths, plus the drafted ledger row(s) and standing repo-ritual instructions — which the designer pastes into a Claude Code session opened on the repository; that session edits the parts in place, appends the ledger row(s), refreshes part timestamps, runs the checks, and commits. Ledger appends replace full-ledger regeneration; the Part 3 history starts a new part at 30 KB instead of ever being trimmed. Conversion behaviour, conflict routing, locking, and constraint 76 are unchanged. | (c) Universal (workflow/meta — governs every future Update Mode run; no conversion-output change) | `11_UPDATE_MODE.md` → Purpose/SS1/4/6/7/8/9 (`11A`) + SS10/11/12/13/14/15/16 (`11B`); `12_CHANGE_LEDGER.md` → When-to-load/Purpose/Feasibility/How-to-maintain (`12A`) + Housekeeping/Notes (`12F`); `00_MASTER_INSTRUCTIONS.md` → Timestamp Convention + Mode 4 (`00A`), File Reference Index rows 11/12 (`00C`), constraints 49/50/51 (`00D`), constraint 76 (`00E`); `_project_instructions_.md` → Mode Triage / File Map rows 11+12 / Output Expectation | Direct-typed (Update Mode) — designer instruction (Chris), 27 July 2026 | Major | Implemented | — |
| CL-0054 | Wednesday, 29th July, 2026 | Component script includes referenced same-level: `../js/…` → `js/…` (Word Find, Crossword, + repo-wide sweep of any other `../js/` include) | (c) Universal | `03` → COMP_04 Crossword + Word Find `Requires:` lines | Finalized difference report (HPRE203, Difference 3) | Major | Implemented | Non-conflicting; not escalated |
| CL-0055 | Wednesday, 29th July, 2026 | Speech-bubble image-column padding sides explicit in constraint 29: left-positioned → `paddingR`, right-positioned → `paddingL` (inner-side padding); never mirror a sibling that pads both sides the same. Partially already in effect — `04C`/`02` side tables predate this row; new content is the constraint-29 wording + anti-mirror clause | (c) Universal | `00` constraint 29; `04C` → Image Column Padding | Finalized difference report (HPRE203, Difference 4) | Major | Implemented | Non-conflicting; not escalated. HPE head-only dialogue exception resolved by CL-0067 (29 July 2026 — deliberate, HPE-head-only-scoped carve-out codified) |
| CL-0056 | Wednesday, 29th July, 2026 | New `09` §4.1 Exclusion 6: correctly red-flagged + visible-fallback unmapped structure later realised by designer with a library component (spider diagram → `shapeHover`; horizontal timeline D&D → `scatter` + `imageLabel`). Reporting suppression only | (e) Ignore always | `09` → §4.1 Exclusion 6 | Finalized difference report (HPRE203, Differences 1–2) | Routine | Implemented | (e)-scope — exclusions list only (`11` §11) |
| CL-0057 | Wednesday, 29th July, 2026 | New `09` §4.1 Exclusion 7: Mode D Convertor-derived filename (kebab-case / `shutterstock-{ID}`) swapped by designer for the actual asset filename (spaced / `shutterstock_{ID}`). Reporting suppression only | (e) Ignore always | `09` → §4.1 Exclusion 7 | Finalized difference report (WJFUN108, Difference 7) | Routine | Implemented | (e)-scope — exclusions list only (`11` §11) |
| CL-0058 | Wednesday, 29th July, 2026 | WJ series: `[MTK Quiz]` outranks a co-occurring in-page quiz tag (`[Multichoice quiz]`, `[Radio quiz]`, …) — activity emitted as the "Go to quiz" button pattern, not the in-page component; a bare in-page quiz tag still builds in-page | (b) Module-series (WJ) | `00` constraint 65; `05` → MTK Quiz Button; `01` → tag taxonomy note; `03` → COMP_02 MCQ note | Finalized difference report (WJFUN107, Difference 1) | Major | Implemented | Non-conflicting (CL-0038 did not address co-occurrence precedence); not escalated |
| CL-0059 | Wednesday, 29th July, 2026 | WJFUN series: `[Audio image]` grids MUST be `audioImage`/`audioImageOption` (audio filename on option `id`), never `audioTrigger` spans wrapping images | (b) Module-series (WJFUN) | `04B` → Audio Image; `02C` checklist | Finalized difference report (WJFUN108, Difference 3) | Routine | Implemented | Non-conflicting; not escalated |
| CL-0060 | Wednesday, 29th July, 2026 | WJFUN Year 4 wordSelect: exactly one `spanValue` span per sentence covering the complete target phrase; qualifying words not tagged separately | (a) Series + level (WJFUN, Year 4) | `03` → COMP_05 Word Select | Finalized difference report (WJFUN204, Difference 3) | Routine | Implemented | Non-conflicting; not escalated |
| CL-0061 | Wednesday, 29th July, 2026 | **No `col-md-8` nested directly inside another `col-md-8`** — inner rows in a `col-md-8` wrapper use `col-md-12 col-12` (or a documented inner pattern); col-8-in-col-12 and col-12-in-col-8 both fine; applies to inner activity `col-8` too. New constraint 77 | (c) Universal | `02B` → Content Rules; `05B` → Columns; `00` → new constraint 77 | Direct-typed (Update Mode) — design-authority instruction (Persephone Samuels, relayed 29 July 2026) | Major | Implemented | Not escalated — direct-typed; Part 1 empty; supersedes never-ledgered documentation only |
| CL-0062 | Wednesday, 29th July, 2026 | **Carousel `.viewer` width is contextual:** nested inside a `col-md-8` wrapper → `col-md-12 col-12`; standalone → `col-md-8 col-12` preferred, `col-md-12 col-12` permitted for large content / book-page images. All carousel types alike. Replaces the fixed "all carousels col-md-8 / never col-md-12" documentation (never ledgered); `14` §14.7 SPELDSA col-md-12 pattern now consistent | (c) Universal | `04A` → Carousel (all sub-type rules + General Carousel Rules); `02B` → Column Class Reference + carousel note; `02C` checklist; `00` constraint 17 | Direct-typed (Update Mode) — design-authority instruction (Persephone Samuels, relayed 29 July 2026) | Major | Implemented | Not escalated — direct-typed; Part 1 empty |
