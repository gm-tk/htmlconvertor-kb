> **Last updated:** Monday, 27th July, 2026 12:34 PM
> **Granular part C (3 of 6) of `00_MASTER_INSTRUCTIONS.md`** — File reference index.
> All sibling parts live in `00_MASTER_INSTRUCTIONS/`; see `INDEX.md` at the repo root. Body below is verbatim source-of-truth content.

<!-- KB-PART-BODY-START -->
## FILE REFERENCE INDEX
 
### Project Files & Sections
| Section | Located In | Contents |
|---------|-----------|----------|
| Section 01 — Template Levels | `01_PIPELINE_EXTRACTION_TAGS.md` | Template HTML tags, head sections, heading patterns, module menus, title patterns (overview vs lesson pages), footers, **reference module derivation** |
| Section 02 — Content Source Formats | `01_PIPELINE_EXTRACTION_TAGS.md` | PageForge `.txt` format, raw Writers Template `.docx` format, MTK `.docx` pointer, **Media List companion document**, **iStock acknowledgements file (API-sourced)**, format conventions (red text, formatting markers, hyperlinks, tables, bullets), content integrity |
| Section 03 — Page Boundaries | `01_PIPELINE_EXTRACTION_TAGS.md` | 4 validation rules, page-to-file mapping, lesson numbering |
| Section 04 — Tag Taxonomy | `01_PIPELINE_EXTRACTION_TAGS.md` | Complete tag list, normalization table, red text handling |
| Section 05 — Tag Interpretation | `01_PIPELINE_EXTRACTION_TAGS.md` | How each tag maps to HTML (structural, headings, media, styling, activities, links) |
| Section 06 — Interactive Data Patterns | `02_DATA_CONTENT_VERIFICATION.md` | 13 data patterns writers use, speech bubble handling |
| Section 07 — Content Rules | `02_DATA_CONTENT_VERIFICATION.md` | Text preservation, grid structure, merging rules, writer perspective |
| Section 08 — Verification | `02_DATA_CONTENT_VERIFICATION.md` | Full checklist, red flag protocol, constraints, edge cases, output specs |
| Template Recognition & Validation | `06_TEMPLATE_RECOGNITION.md` | Legacy vs Refresh detection, Refresh sub-type identification (Standard/Bilingual/Fundamentals/Inquiry/Combo), structural norms per sub-type, known quirks and pitfalls in reference files, Mode B validation checklist, Refresh baseline element reference |
| MTK Docx Conversion | `07_MTK_DOCX_CONVERSION.md` | MTK (Te Reo Rangatira) direct docx-to-HTML conversion, bilingual extraction, embedded skeletons, acknowledgements on page 0.0 |
| Module Support, Advisory & Debugging | `08_MODULE_SUPPORT_DEBUGGING.md` | Mode 2 — answering advisory questions from the knowledge base, completing half-finished modules, debugging interactives, one-off module overrides, scope boundaries, mode triage |
| Comparison Mode | `09_COMPARISON_MODE.md` | Mode 3 — the `COMPARISON MODE` trigger (Phase 1) and the `number-letter` pairing trigger (Phase 2), the four required inputs (raw content + structural reference + original generated HTML + designer-refined HTML), the **inclusion gate** (report ONLY knowledge-derived differences; filter out template-derived output lifted from the supplied reference/example/templated file — R is used to draw this line), the **single-report architecture**, continuous difference numbering, the **streamlined three-section Phase 1 bundle** (raw content → originally generated code → designer's refined code, source withheld) and the **detailed four-section Phase 2 bundle** (adds the cited source rule), the **five scope options listed once at the top** (a series + level — this subject series at this level only, b module-series — whole series across all levels, c universal, d ignore-once, e ignore-always), the two-stage flow (designer replies with `number-letter` pairings → finalized detailed report), the (d)→omit and (e)→keep-with-future-exclusion-instruction handling, the `[LEVEL_DESCRIPTOR]` resolution + template-level → descriptor mappings, the rule that **Comparison Mode never regenerates the project files** (actioning happens in **Update Mode** — see `11`), the downloadable report |
| Update Mode | `11_UPDATE_MODE.md` | Mode 4 — the `UPDATE MODE` trigger, **format-agnostic intake** (finalized difference report, free-typed instructions, bullet/one-line/uploaded changes — any format), normalisation into discrete scoped changes, the shared five-scope vocabulary reused from `09` (a/b/c propagating, d ignore-once = no edit, e ignore-always = add a `09` Section 4.1 exclusion), the **conflict check** against the change ledger (a request contradicting a **locked** decision is blocked), **Major vs Routine classification**, the **conflict-escalation & locking gate** (a report-vs-report conflict is catalogued for the design authority Persephone to resolve, then the surviving decision becomes `Locked` and immutable; non-conflicting changes are actioned directly), **pre-flight confirmation** of scope and any guardrail-weakening change, the **blast-radius sweep** (find *every* owning rule + cross-reference + constraint-list entry + index entry + ledger row + project-instructions touchpoint), the **Repo Update Brief** (the run's sole deliverable — precise, finished edits with granular part paths + drafted ledger rows, pasted into a Claude Code session on the `htmlconvertor-kb` repository, which edits the parts in place, runs the repo checks, and commits; **no file regeneration, ever** — `CL-0053`), the **project-wide timestamp convention**, and the explicit boundaries (edits rule files only — never student content, never converted HTML, never invents un-requested changes, never overrides a lock without authority sign-off, **never produces a difference report for the designer** — constraint 76). This is the downstream actioning conversation `09` → Section 10 hands off to |
| Change Ledger | `12_CHANGE_LEDGER.md` | The **in-house**, append-only **conflict & lock registry** Update Mode reads at the start of every run; new rows are drafted into the Repo Update Brief and appended by the linked Claude Code session (never regenerated in full) — no external spreadsheet or manual upkeep. Organised in three parts so conflict-relevant data stays compact: **Part 1 — Locked Decisions** (binding/immutable; the conflict check reads this first), **Part 2 — Pending approval** (report-vs-report conflicts awaiting the design authority's resolution), and **Part 3 — Change History** (the full append-only audit log of every actioned change with ID, date, summary, scope, affected files/rules, source, Routine/Major class, status, and authority decision). Feasible at any size because conversion chats never read it and project knowledge is retrieved (not bulk-loaded); the growing Part 3 is inert audit data |
| Split Mode | `13_SPLIT_MODE.md` | Mode 5 — the `SPLIT MODE` precedence trigger, the **proactive single-page identification + offer** during page-structure determination, the rule that it is a **packaging variant of Conversion** (all conversion rules unchanged), the two outputs (**base homepage** `<CODE>-base.html` with `#body` = ordered `PAGEFORGE-SPLICE` markers only, header/menu/footer/acks fully built, no `stickyNav`; and one **section file** `<CODE>-lesson-<id>.html` per slot, section-marker-wrapped raw `#body` content including the `<!-- N -->` delimiter), the id/filename conventions (`intro` then `01`/`02`…, case-insensitive, unique), the **exact marker contract** (`PAGEFORGE-SPLICE` / `PAGEFORGE-SECTION` / `/PAGEFORGE-SECTION`, reproduced verbatim), the **one-file-per-response emission cadence** (base first, then one section file per designer prompt, in order, never more than one file per turn), the **`PAGEFORGE-GUIDE-START` … `PAGEFORGE-GUIDE-END` manual-stitch guidance blocks** (highly detailed human-developer stitch instructions — base block at each splice point naming the section + order, section block outside the content markers naming the splice point it fills — stripped by the Stitcher), the **round-trip guarantee** (stitched `#body` byte-identical to a one-pass build, no markers survive — splice markers and guide blocks alike), the **validation/failure handling** (one section per slot, ids one-to-one, no duplicates, ≥1 slot, missing content → visible red flag in the section file never an empty slot), how PageForge's Page Stitcher consumes the output, and the carried-over hard constraints. Distinct from the multi-page **Page Boundary System** (`01` Section 03) — the two are never conflated |
| Subject Global Parameters | `14_SUBJECT_GLOBAL_PARAMETERS.md` | The design team's per-subject **global-parameter** conventions, folded in so the Convertor applies them automatically instead of a developer eyeballing a reference doc. One **scope-tagged** section per family: **Languages Phase 1–4** (cohort — all-combo `template="combo"`, central `imageCentral` character assets, two-tile Novice/Emergent Fundamentals, red-bracket set characters, manual video insertion, sticky nav), **Pathways** (a — Level 1: Sections nav, 3-lesson structure, persona tagging, K/P + overview deferred; Level 2 held), **Taonga** (cohort — FUN-style intro + clickable lessons with dropboxes, A–F lesson layout, phase-keyed `tui_characters/tui_phase_N` markup, Tui art + overview deferred), **CED Phase 5** (a — 3-tab overview, dictionary box/sticky-nav code, wānanga/talanoa `alert cultural layout="combined"`, flipCard `.noBG`, PDF resources deferred), **FUNdamentals H&PE** (cohort — refines the `06` Fundamentals sub-type: 5–6 RHS tabs, intro/reflection, one engagement quiz), **LS** (b — `.learningSupport`, larger font, terminology/brackets, no tab navs, clickDrop layout, speech-bubble prompts, 6-activity pattern, XLP unique overviews), **BLL** (b — level split: **BLL2xx phase 1 `template="1-3"`**, other BLL `template="4-6"`, Sassoon font, page-1 animated intro, elkonin boxes, green boxes, **`.dropbox` NOT on `.activity` carve-out**, book-style story carousel, minutes/seconds timer), **HPE content** (cohort — overview=intro, lesson-summary alert, sticky nav on Fundamentals+Help, checkpoint + end-of-module celebration with phase-specific copy, character system), **BLLR** (b — phase 2 `template="4-6"` (BLL2xx exception never applies), bookworm avatar keyed to module-code range, art/audio deferred, secret shelf + Whispering Archives red note; **distinct prefix from BLL**; parameters still incomplete), **MiW/WJ** (cohort — `WJ` prefix, trailing-`0` FUN vs non-zero module test, NZ map `imageLabel layout="map"` on every intro page, FUN intro + clickable lesson tiles, per-lesson side-nav tabs, phase-varying Kea character with speech bubble + audio, art/audio deferred). Cross-cutting: the **"Upload to Dropbox" series label** (BLL/LS/HPE) alongside the universal "Go to dropbox"; deferred items emitted as visible **`Designer/Developer To Do:`** placeholders. Scope-bound (a convention applies only within its scope); **Mode B sibling authority still wins** on structure; introduces **no new CSS/JS or invented classes** |
 
### Component Sections
| Section | Located In | Components Covered |
|---------|-----------|-------------------|
| COMP_00 — Index & Universal Rules | `03_COMP_CORE_INTERACTIVES.md` | Universal rules, component whitelist, button class reference, deprecated list |
| COMP_01 — Drag & Drop | `03_COMP_CORE_INTERACTIVES.md` | Drag & Drop (all 6 layouts + modifiers) |
| COMP_02 — Quizzes | `03_COMP_CORE_INTERACTIVES.md` | Dropdown Quiz, MCQ, multiChoiceQuiz (Survey/Self-Assessment `checkAll` AND graded multi-select `mcqSomeSelected`), Radio Quiz, Typing Quiz |
| COMP_03 — Self Check | `03_COMP_CORE_INTERACTIVES.md` | Self Check, Self Reflection, Reflection Slider |
| COMP_04 — Games & Words | `03_COMP_CORE_INTERACTIVES.md` | Memory Game, Puzzle, Crossword, Word Find, Bingo, Word Drag |
| COMP_05 — Ordering & Selecting | `03_COMP_CORE_INTERACTIVES.md` | Reorder, Clicking Order, Word Select, Checklist/Selection Box |
| COMP_06 — Sliders | `03_COMP_CORE_INTERACTIVES.md` | Slider (Scale/Survey), Slider Chart |
| COMP_07 — Content Segmentation | `04_COMP_SEGMENTS_OVERLAYS.md` | Accordion, Carousel, Rotating Banner, Click Drop, Flip Card, Tabs, Hint, Hint Slider, Modal |
| COMP_08 — Triggers & Overlays | `04_COMP_SEGMENTS_OVERLAYS.md` | Info Trigger, Audio Trigger, Image Label, Image Zoom, Word Highlighter |
| COMP_09 — Speech Bubbles | `04_COMP_SEGMENTS_OVERLAYS.md` | Speech Bubbles (all variants, colours, audio) |
| COMP_10 — Diagrams & Timelines | `04_COMP_SEGMENTS_OVERLAYS.md` | Shape Hover, Timeline, Venn Diagram |
| COMP_11 — Drawing Tools | `04_COMP_SEGMENTS_OVERLAYS.md` | Sketcher, Number Line, Stop Watch |
| COMP_12 — Language | `05_COMP_LANGUAGE_MEDIA_LAYOUT.md` | Glossary, Kanji Cards, Language Fonts, Translate Section, MathJax |
| COMP_13 — Media & Embeds | `05_COMP_LANGUAGE_MEDIA_LAYOUT.md` | Video Embed, Audio Player, Embed PDF, Embed Padlet, Embed Desmos |
| COMP_14 — Layout & Structure | `05_COMP_LANGUAGE_MEDIA_LAYOUT.md` | Activities, Alerts, Buttons, Supervisor Button, Tables, Columns, Quotes, Whakatauki, Acknowledgements, Rhetorical Question |
 
---
 
